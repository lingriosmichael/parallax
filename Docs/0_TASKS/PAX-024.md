TASK: PAX-024
TITLE: Overlap pressure plate (A) opens a gate (B); HUD Echo timeline; Gate 3 on the phone
PHASE / GATE: Phase 4 / Gate 3

OBJECTIVE
The first Type B (sustained) solo puzzle. Cat A records itself standing on a pressure plate,
switches, and A's Echo holds the plate so the gate in Reality B stays open while you walk Cat B
through. The plate uses an overlap query, so it detects live and Echo cats alike. A small HUD
timeline shows the Echo's progress. Then all seven Gate 3 checks (plan §10.6) run on the Pixel 8a,
together with the Play-mode and device tests deferred from PAX-021 and PAX-023.

CONTEXT
- 02_ARCHITECTURE.md §7.2 (presenters/manifestations), §8.4 (playback), §8.5 (sensors must detect
  Echoes), §9.1, §13.
- Plan §10.2 (Echo flow), §10.4 Type B, §10.6 (Gate 3 checks).
- D-022 (tick ownership), D-024 (per-origin sequences), D-025 (presenters, TransportHost,
  AnchorDefinition), D-026 (solo stays), D-027 (Echo v1).
- Current state: PAX-023 committed; 94 EditMode tests green; PAX-021 and PAX-023 Play-mode and
  device tests not yet run.

DESIGN DECISION IN THIS TICKET (record as D-028)
An **Inactive cat does not press plates**. When you switch away without recording, the cat you left
"dozes": it still stands and falls under gravity, but sensors ignore it. Only LocalHuman and
EchoReplay cats count. Without this rule, Type B puzzles are solvable by simply switching away
while standing on the plate, and Echo loses its purpose. RemoteHuman cats are also not counted:
in co-op a reality's sensors run on the device that owns that reality's cat.

---

STEP 0 — BEFORE CLAUDE CODE STARTS
`git status` clean and up to date with origin/main (PAX-023 committed and pushed).
Claude Code: check. If not true, stop and report.

---

CLAUDE — CODE

PRE-READ (report before editing; stop if anything contradicts this ticket)
a. Where `InputSourceKind` is declared (assembly and namespace) and its members.
b. `ObserverContext.Driver` (can it be null?) and how `ObserverContext.Cat` exposes the cat's
   `Collider2D`, if at all.
c. `ElevatorManifestation`: confirm it can be reused unchanged for a vertical gate (two local
   positions and a speed, kinematic, collider).
d. The helper methods in `AnchorSetup` and `EchoSetup` (compare-before-set, EnsureChild,
   SetVisual). This ticket moves the ones it needs into a shared `SetupUtility`.
e. The names and bounds of Reality B's left wall / Ground colliders, so the gate and goal
   marker fit between the wall and the spawn point.

Create — Parallax.Core
- Core/Sensors/SensorLatch.cs (pure C#):
  - Constructor `(float initialValue)`: the latched state starts as `initialValue >= 0.5f`.
  - `bool Update(bool occupied, out float target)`: if `occupied` differs from the latched
    state, latch it, set `target` to 1 or 0, and return true; otherwise return false.
- Modify Core/Anchors/EventOrigins.cs: add `Sensor(ObserverId reality) => Human(reality)`, with a
  comment: sensor requests use the origin of the device that owns the reality (D-028).
- If `InputSourceKind` lives in Core: add Core/Sensors/SensorPolicy.cs,
  `public static bool Counts(InputSourceKind kind)` → true for LocalHuman and EchoReplay, false for
  Inactive and RemoteHuman. If it lives in Gameplay, put the same class in Gameplay/Sensors and say so.

Create — Parallax.Gameplay (namespace Parallax.Gameplay.Sensors)
- Gameplay/Sensors/PressurePlateSensor.cs — MonoBehaviour under a RealityRoot.
  - Serialized: `AnchorDefinition definition`, `TransportHost transportHost`,
    `ObserverSet observers`, `Vector2 sensorSize = (1.2, 0.6)`, `Vector2 sensorOffset = (0, 0.3)`,
    `SpriteRenderer indicator`.
  - Lazy `EnsureInit()` (from OnEnable and the tick): RealityRoot via `GetComponentInParent`,
    `SensorLatch` from `definition.InitialValue`, reused `List<Collider2D>` results, and a
    ContactFilter2D using the root's `PhysicsMask` with `useTriggers = false`. Log a
    context error once if anything is missing.
  - Subscribes to `ObserverSet.Stepped` in OnEnable, unsubscribes in OnDisable. It never uses
    FixedUpdate or trigger callbacks.
  - Per tick:
    1. `ObserverContext ctx = observers.Get(root.Id)`.
    2. `occupied` is true only if ctx, ctx.Cat and ctx.Driver are non-null,
       `SensorPolicy.Counts(ctx.Driver.Kind)`, and
       `Physics2D.OverlapBox(center, sensorSize, angle, filter, results)` contains that cat's
       collider. The center is the transform position plus the rotated offset; the angle is the
       transform's z rotation. Cache the cat's collider; refetch only if ctx.Cat changes.
    3. `if (latch.Update(occupied, out float target)) requester.Request(definition.Id, target);`
       The requester is an `AnchorRequester(transportHost.Transport, transportHost.Sequencer,
       EventOrigins.Sensor(root.Id))`, created lazily.
    4. Local feedback: `indicator` is bright while `occupied`, dim otherwise, set immediately
       (before the commit). Only assign the color when it changes.
  - Never touches manifestations, the registry, or the CatInteractor. Its requests are not
    recorded by Echo: during replay the plate detects the Echo's body instead.
  - `OnDrawGizmosSelected`: draw the sensor box.
- Gate in B: reuse `ElevatorManifestation` unchanged (closed = `loweredLocal`, open = `raisedLocal`).
  Record the misleading class name as tech debt in the handoff; do not rename it in this ticket.

Create — Parallax.Gameplay (namespace Parallax.Gameplay.UI)
- Gameplay/UI/EchoTimelineView.cs — uGUI, non-interactive (`raycastTarget = false` on its graphics).
  - Serialized: `ObserverSet observers`, `SoloSwitchController switchController`,
    `GameObject bar` (child shown/hidden), `Image fill` (Image.Type.Filled, horizontal),
    `Text label`.
  - Update: the Observer that isn't active; if its driver is `EchoReplayDriver replay`, show `bar`,
    `fill.fillAmount = Mathf.Clamp01(replay.Cursor / (float)replay.FrameCount)`, and label
    `ECHO A 3.2/6.0` (seconds = frames × fixedDeltaTime, one decimal) or `ECHO A HOLD` while
    holding. Otherwise hide `bar`.
  - Assign fillAmount, text and SetActive only when the value changes (text by tenths).

Modify — Parallax.Editor
- Editor/Setup/SetupUtility.cs: move the compare-before-set helpers this ticket needs into a shared
  static class (EnsureChild with layer, Ensure<T>, SetLocalPosition, SetColliderSize, SetBodyType,
  SetObject, SetVector2, SetFloat, SetVisual, SetArray, EnsureSceneRoot, EnsureDefinition(path, id,
  displayName)). One statement per line. Existing setup scripts may keep their private copies; do
  not change their behavior.
- Editor/Setup/PlateGateSetup.cs, menu `PARALLAX/Setup/Plate + Gate (Sandbox)`, idempotent,
  compare-before-set. Heights as in AnchorSetup (`groundTop`, `jumpHeight` from
  CatMotorConfig.JumpHeight, `spawnX`). It creates or ensures:
  1. Asset `Assets/_Game/Data/Anchors/Anchor_Gate01.asset`: id 2, initialValue 0, displayName "Gate01".
  2. Reality A: `RealityRoot_A/Anchors/Plate_A` at local (spawnX − 2.5, groundTop), with a
     PressurePlateSensor wired to the definition, `Systems/LocalTransportHost` and ObserverSet.
     Its child `Indicator` is a sprite 1.2 × 0.15 (A_Gameplay), local y = 0.075. No collider:
     the plate is flush with the ground.
  3. Reality B: `RealityRoot_B/Anchors/Gate_B` with ElevatorManifestation, a kinematic
     Rigidbody2D, BoxCollider2D 0.5 × gateHeight, and a sprite sized to match (B_Gameplay), layer
     RealityB. Its x is spawnX − 2.5 and `gateHeight = 1.6 × jumpHeight`.
     `loweredLocal` (closed) = (x, groundTop + gateHeight / 2);
     `raisedLocal` (open) = (x, groundTop + gateHeight × 1.5 + 0.2); `speed` = 4.
  4. Reality B: `RealityRoot_B/Geometry/Goal_B`: visual-only sprite 0.4 × 1.2 (B_Gameplay), at
     x = spawnX − 4.5, sitting on the ground. If the goal or gate would overlap the left wall
     bounds from pre-read (e), move both right together, keeping the gate at least 1.0 left of
     spawnX, and log it. If they can't fit, log an error and stop before creating B objects.
  5. Scene root `Anchors/Presenter_Gate01`: RealityPresenter with the definition, host, and
     manifestations [Gate_B].
  6. HUD: `HUD/EchoTimeline` (anchored top-center, below SWITCH/REC), with child `Bar` holding a
     background Image, a `Fill` Image (Filled, horizontal) and a `Label` Text. Wire
     EchoTimelineView. Size about 360 × 36; ASCII text only.
  Logs every change and computed position; a second run logs "PlateGateSetup: no changes."
  Runs RealityIsolationValidator and AnchorValidator.
- Modify Editor/Setup/AnchorValidator.cs:
  - Every PressurePlateSensor has a definition, a host and an ObserverSet, is under a RealityRoot
    with a matching layer, and a presenter exists for its id.
  - One writer per anchor: no anchor id is used by more than one VineInteractable or
    PressurePlateSensor (together).

Create — Parallax.Tests.EditMode
- SensorTests.cs, one behavior per test:
  - SensorLatch, initial 0: Update(false) → false; Update(true) → true with target 1;
    Update(true) → false; Update(false) → true with target 0.
  - SensorLatch, initial 1: Update(true) → false; Update(false) → true with target 0.
  - SensorLatch flicker: true, false, true → three changes with targets 1, 0, 1.
  - SensorPolicy: LocalHuman and EchoReplay count; Inactive and RemoteHuman don't.
  - EventOrigins.Sensor(A) == HumanA and Sensor(B) == HumanB.
  - Integration: AnchorRegistry + LocalTransport (tick source) + one EventSequencer +
    AnchorRequester(Sensor(A)). Occupied during tick 5 → request → committed at tick 6, value 1;
    unoccupied during tick 7 → committed at tick 8, value 0; Revision 2, both results Applied.
  - Shared origin: a CatInteractor-style AnchorRequester(Human(A)) and the sensor requester built on
    the same sequencer issue strictly increasing sequences (1, 2, 3 interleaved), so neither is
    ever Duplicate.

Docs
- 02_ARCHITECTURE.md
  - §8.5: "As built (PAX-024)": sensors step on `ObserverSet.Stepped`, use `OverlapBox` with the
    reality mask against that reality's cat, count only LocalHuman/EchoReplay, request on change
    through an `EventOrigins.Sensor` requester, and are not recorded by Echo.
  - §4.2: one line saying an Inactive cat does not press sensors (D-028).
  - §13: the HUD Echo timeline.
- 07_DECISIONS.md, append:
  ### D-028 · <date> · Accepted
  **Decision:** (1) Puzzle sensors run an `OverlapBox` query on `ObserverSet.Stepped` with their reality's mask and count that reality's cat only when its driver is `LocalHuman` or `EchoReplay`. Inactive cats do not press sensors; RemoteHuman cats are not counted on this device. (2) Sensors request on state change only, through their own `AnchorRequester` with origin `EventOrigins.Sensor(reality)` (= that reality's Human origin) and the device's single sequencer. (3) Sensor requests are not recorded by Echo; during replay the sensor detects the Echo body. (4) Each anchor has exactly one writer (validator-enforced).
  **Why:** If Inactive cats pressed plates, every sustained puzzle could be solved by switching away while standing on the plate, making Echo pointless. In co-op a reality's cat is simulated only on its owner's device, so only that device may report its sensors, and its Human origin already belongs to that device's sequencer (a shared System origin from two devices would collide under D-024). Recording sensor requests as well would double-issue them on replay.
  **Consequence:** Level design: a cat left Inactive is scenery for sensors but still collides and falls. Networking (PAX-033): sensors must be disabled for realities this device does not own.

---

YOU — UNITY EDITOR (walked through one step at a time)
1. Recompile; zero errors. EditMode tests all green.
2. Sandbox_Realities: run `PARALLAX/Setup/Plate + Gate (Sandbox)` twice (second run: no changes). Save.
3. Editor acceptance tests (below), including the deferred PAX-021/023 ones.
4. Development Build to the Pixel 8a (the debug panel is compiled only in development builds);
   run the Gate 3 device checks.

DO NOT
- Rename ElevatorManifestation or change its behavior.
- Add timed plates, weight, multiple cats per plate, or multi-writer anchors.
- Record sensor requests in Echo, or change EchoSession/EchoReplayDriver.
- Reset anchors on respawn (still the checkpoint ticket).
- Change motor, input, switch, transport or reality code.
- Add singletons, static mutable state, or runtime Find. Hand-edit .unity/.prefab/.asset/.meta.
- Commit. The Architect reviews first.

REQUIREMENTS
- The plate detects live and Echo cats identically, via overlap queries only.
- The plate never reacts to an Inactive cat.
- Plate requests go only through its sensor requester, on change only.
- The indicator responds on the tick the cat enters or leaves; the gate moves only after commit.
- The timeline is visible only while an Echo exists, and allocates nothing per frame once stable.
- Validators pass; setup is idempotent; zero new Console warnings.

ACCEPTANCE TEST

Editor — deferred PAX-021 (latency 0 unless stated)
1. Panel shows Anchor#1 and Anchor#2, both value 0.00 rev 0, before interacting.
2. As A, F in the vine zone: pulse, knot drops, Anchor#1 rev 1. F away from it does nothing.
3. Tab to B: elevator raised; jump onto it, then the ledge (unreachable from the ground).
4. Inactive guard: A in the vine zone, Tab to B, F → Anchor#1 unchanged.

Editor — deferred PAX-023
5. Z, walk, jump, F in the vine zone twice ~2 s apart, Tab. A replays from where recording
   started (PiP on to watch), Anchor#1 rev rises by 2 more, LastCommitResult never Duplicate.
6. Tab back mid-replay: A continues from the Echo's position under your control.
7. Record past 10 s (REC FULL), Tab: replay then HOLD. Z does nothing while the Echo exists (REC --).
8. Z, move, Z again, Tab → A is Inactive (discarded).

Editor — PAX-024
9. Walk A onto the plate: the indicator brightens immediately; Anchor#2 → 1; in B (Tab or PiP)
   the gate rises. Step off: dims, Anchor#2 → 0, gate closes.
10. Inactive rule: stand A on the plate, Tab without recording → the gate closes (A dozes).
11. Type B solve: as A, stand beside the plate, Z, walk onto the plate, stand ~3 s, Tab.
    The timeline appears (`ECHO A x/y`, filling). The gate closes briefly (the Echo restarts from
    beside the plate), then opens when the Echo steps on, and stays open after the replay ends
    (`ECHO A HOLD`). Walk B through the gate to the goal marker.
12. Timed variant: Z, walk onto the plate, stand ~3 s, walk off, Tab. The gate opens for about
    3 s during the replay and closes again; B must be through in time.
13. Tab back to A while the Echo holds the plate: A is live on the plate, the gate stays open, and
    the timeline hides.
14. Latency 300: step on the plate → indicator immediate, gate ~0.3 s later.
15. Zero warnings and errors throughout. Movement, switching, gravity debug unchanged.

Device (Pixel 8a, Development Build) — Gate 3, plan §10.6
16. (Checks 1–3) One phone holds A and B; SWITCH changes control; each shows its own reality.
17. (Checks 4–5) Vine with touch interact → SWITCH → elevator raised; ride to the ledge.
18. (Check 6) Test 11 with touch at latency 300, three times in a row. The Echo takes the same path
    and the gate opens and closes at the same moments each time.
19. (Check 7) Everything above runs on LocalTransport; the grep shows no Photon/Fusion references.
20. Touch safety: tapping REC, SWITCH, DBG or the panel never moves, jumps or interacts; tapping
    interact never jumps; the stick and jump still work.
21. Test 12 at latency 300 with touch.

DELIVERABLE (from Claude Code)
Standard CLAUDE.md output, plus:
- pre-read report (a–e);
- a tick walk-through at latency 0: the Echo's frame that first overlaps the plate is applied on
  tick T (MovePosition, so the body arrives during that tick's physics step). On which tick does the
  sensor see it, request, and the commit land?
- every position PlateGateSetup computes (from its log, or the formula if untested);
- grep proof, verbatim:
    grep -rn "Photon\|Fusion" Assets/_Game
    grep -rn "OnTrigger\|OnCollision" Assets/_Game/Gameplay/Sensors
    grep -rn "Find" Assets/_Game/Core Assets/_Game/Gameplay
    grep -rn "?\." Assets/_Game/Gameplay/Sensors Assets/_Game/Gameplay/UI/EchoTimelineView.cs
- D-number used; new EditMode test count.
- Review file:
    { git status; git --no-pager diff; cat <every new .cs file>; } > ~/Desktop/pax024_review.txt 2>&1

DEFINITION OF DONE
Universal DoD, plus:
- You can explain in two sentences why sensors use overlap queries instead of trigger callbacks,
  and why an Inactive cat must not press plates.
- All Gate 3 device checks (16–21) pass on the Pixel 8a, plus Editor tests 1–15.
- Architect review passed before commit:
  `PAX-024: overlap pressure plate, gate, HUD Echo timeline (D-028); Gate 3`
- Gate 3 is recorded as passed in the handoff only after tests 16–21 pass on the phone.
