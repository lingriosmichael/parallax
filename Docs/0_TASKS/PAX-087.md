# PAX-087 · KIT-8: climbable vines (stick up/down, leap off)

**Status:** Approved for implementation. **Wait after Phase 1:** post the trace and stop.
**Phase 1 size: Full.** New input axis on every input source, a new motor state, and a route-format change.
**Phase:** Solo v1, kit for levels 11+ (KIT-8).
**Depends on:** PAX-086 committed (the motor's `ApplyLaunch` seam exists). Baseline = PAX-086's total.
**Decisions:**
- D-021/D-049: the virtual stick; movement screen-relative. D-022/D-023: the input rig and reserved regions.
- D-048: gravity vertical only. D-052: the cat collider. D-073: pause and `ResetTransientState`.
- D-077: coyote/buffer. D-079 (1): the route format. D-081 (4): route moves are full-magnitude. D-087: inversion.
- New **D-089**, written from this ticket as built.

**Implementer reads first:** `CLAUDE.md`; the decisions above; `CatCommand`, `VirtualStick`, `TouchStickCatInput`, `KeyboardCatInput`, `CatInputRouter`, `LocalHumanDriver`, `CatMotor2D`, `JumpWindows`, `Route.cs`, `RouteSession`/`RouteValidator`, `CatVisualPresenter`/`CatAnimStateMachine`.
**Frozen, do not touch:** `VineInteractable`, `VineManifestation`, `AnchorSetup` vine code (co-op anchor vines, D-025). Name the new classes `ClimbVine…` so nobody confuses the two.

**Menus are the developer's job.**

---

## 1. Why

Vines turn walls into routes: climb up, then leap off to a platform. The art already exists (`Assets/_Game/Art/RealityA/Environment/A_OBJ_Vine.png`, 236 × 433). The developer chose stick up/down to climb, so the input gets a vertical axis for the first time.

## 2. What to build

1. **Climb axis.** `CatCommand` gains `float Climb` (−1..1, **screen-up positive**, D-049). Default 0, so every existing path is unchanged.
   - Touch: the stick's y through `VirtualStick` with its own dead zone (default 0.35, higher than x's 0.15 so a slightly diagonal run never reads as climbing).
   - Keyboard: W/S and Up/Down.
   - `CatInputRouter` merges it like `Move`. Echo (frozen) ignores it; confirm in Phase 1.
2. **Element `Vine`** (appended): x centre, bottom y, height, width 0.6 (grab box). Built with the vine sprite tiled vertically (`SpriteDrawMode.Tiled`) if Phase 1 confirms the importer allows it; otherwise a placeholder colour and a note for the art ticket.
3. **Climbing state** (pure `ClimbState` in Core, small `CatClimber` in Gameplay, consulted by the motor):
   - **Grab:** the cat's collider overlaps a vine and `|Climb| ≥ 0.5` (grounded or airborne). No automatic grab: touching a vine without pushing up/down does nothing.
   - **While climbing:** the motor applies no gravity; x snaps to the vine centre on the grab tick; vertical velocity = Climb × `ClimbSpeed` (default **4 u/s** = 0.08 u/tick), screen-relative; the cat can't climb past the vine's top or bottom (it stops there). `Move` alone does nothing.
   - **Leap off:** a jump press while climbing detaches with the normal jump launch (1.6 u apex) and horizontal velocity `sign(Move) × MaxSpeed` (straight up if Move is 0). `Move` is read *after* the inverter (D-087), so an inverted cat leaps the other way; `Climb` is never inverted.
   - **Release:** climbing down past the bottom, a gravity flip, a death, a room reset, a respawn or a launch (`ApplyLaunch`) releases the vine. After a release or leap the cat can't regrab the same vine for `RegrabLockTicks` (default 10).
   - Coyote after a leap: none (like a jump). Buffered jump on the grab tick: leaps.
4. **Snap trap (optional per vine):** a vine can take trap settings (Overlap trigger or Chain, Once, `DelayTicks`). When it fires, it vanishes and releases the cat (a fake-platform-style reveal, D-080 (1)). Resets with the room.
5. **Presentation:** a climb pose in `CatAnimStateMachine` (reuse Walk frames rotated or the Idle frame until the art ticket delivers a climb sheet; no Animator, D-036).
6. **Routes.** `Hold(Up)`, `Hold(Down)`, `ReleaseClimb()` and condition `Climbing()`; the per-tick record gains `IsClimbing`. Existing routes replay byte-identically (Climb = 0).
7. **Validator:** `ValidateVine` (inside the frame, overlaps no solid, height ≥ 1.5, bottom reachable: on a floor or within a jump's reach), `ValidateBand` (levels 11+ only). Leaps off a vine are timed/route-proven, not static `RequiredJump`s, unless Phase 1 finds a cheap way.
8. **Trap Lab room 9:** a tall wall with two vines. The obvious vine snaps halfway up (Dies in the pit below, lead ≥ 6); the real one reaches a ledge you leap onto. Solution + one Dies + one Recovers.

## 3. Phase 1 (Full, ≤ 150 lines; at most 6 questions)

1. Every consumer of `CatCommand` (drivers, Echo recorder, `SoloSwitchController`, debug) and whether adding a field changes any of them.
2. `TouchStickCatInput`: where the stick's y is available today, and whether reserved regions or the jump button zone interfere with upward pushes.
3. The motor: where the climbing branch goes so the non-climbing path stays byte-identical (prove with the existing harness pins, D-082 (6)).
4. The vine sprite: can `SoloRoomBuilder` draw a tiled sprite without touching the `.png.meta` importer settings? If not, placeholder colour now.
5. Route format: the smallest change to `Route.cs` that adds vertical holds without changing existing route files.

## 4. Rules and stop conditions

- Existing routes and harness pins unchanged (Climb = 0 everywhere). A changed pin is a stop condition.
- No Unity package installs. No Animator. Frozen vine anchor code untouched.
- If a diagonal run on the stick can grab a vine unintentionally at the chosen dead zone in the harness, stop and report.

## 5. Tests (seen red first)

- `VirtualStick` y axis and dead zone; keyboard W/S; router merge.
- `ClimbState` pure: grab conditions, speed, top/bottom stops, leap velocity with/without Move, inverted leap, regrab lock, release causes.
- Harness: grab from ground and from a jump, climb to top, leap to a ledge; gravity flip releases; death/respawn/reset clear climbing; pause/hold freeze it.
- Snap vine fires, releases, resets. Validator clauses, `ValidateBand`, room 9 routes, camera tell rule.
- Every existing route and `RouteHarnessFidelityTests` pin unchanged.

## 6. Allowed files (Phase 1 confirms)

`CatCommand.cs`, `VirtualStick.cs`, `TouchStickCatInput.cs`, `KeyboardCatInput.cs`, `CatInputRouter.cs`, `LocalHumanDriver.cs`, `CatMotor2D.cs`, new `ClimbState.cs` (Core), new `CatClimber.cs` and `ClimbVine.cs` (Gameplay), `CatAnimStateMachine.cs`/`CatVisualPresenter.cs` (climb pose only), `SoloRoomElementTypes.cs`, builder/`TrapKitSetup.cs`, `LevelLayoutValidator*.cs`, `Route.cs`, `RouteValidator.cs`/`RouteSession.cs`, `TrapLabLayout.cs`, `TrapLabRoutes.cs`, tests, `07_DECISIONS.md` (D-089), `08_V1_ROADMAP.md`.

## 7. D-089 (write as built)

Climb axis (screen-up, dead zone), vines, grab/climb/leap/release rules and defaults, no gravity while climbing, regrab lock, snap vines, route format additions, levels 11+ only, room 9 measurements. Consequence: `CatCommand` has two axes; D-081 (4)'s magnitude gap now covers Climb too.

## 8. Out of scope

Ropes/swinging · climbing on walls without a vine · vines in levels 1–10 · final climb animation (art ticket) · device tuning of the dead zone (Phase H).

## 9. YOU: Unity Editor

Rerun the Trap Lab menu, save, play room 9 with the keyboard and with the Device Simulator's touch stick: grab, climb, leap to the ledge, fall once from the snapping vine. Say whether 4 u/s climbing and the 0.35 dead zone feel right (feel on device is Phase H).

## 10. Required output

CLAUDE.md's list, plus proof that existing pins didn't move, room 9 measurements, and the new EditMode total.
