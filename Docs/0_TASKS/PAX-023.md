TASK: PAX-023 (absorbs the playback core of PAX-024)
TITLE: Echo v1: RECORD, state-based recording, kinematic playback, hold and cancel
PHASE / GATE: Phase 4 / toward Gate 3

OBJECTIVE
A solo player records up to 10 s as one cat, presses SWITCH, and that cat's Echo replays the
recording from its start while the player controls the other cat. The Echo re-issues its anchor
requests on the recorded ticks, so Cat A's Echo can operate the elevator Cat B is standing on.
The Echo holds its final pose when the recording ends; switching back cancels it.

SPLIT
- PAX-023 (this ticket): Echo data, recorder, playback driver, RECORD button and key, switch
  integration, debug panel rows. EditMode tests.
- PAX-024 (next): overlap-query pressure plate opening a gate in B (Type B sustained puzzle),
  HUD Echo timeline, Gate 3 on the phone.

CONTEXT
- 02_ARCHITECTURE.md §4.2 (solo switching), §8 (Echo, state-based), §9.1, §10 (time).
- Plan §10.2 (Echo flow v1), §10.4 (puzzle types), §10.6 (Gate 3 checks).
- D-022 (ObserverSet owns the tick; motor has no FixedUpdate), D-023 (SoloSwitchController is
  the only runtime owner of drivers), D-024 (Echo replays need fresh sequences), D-025
  (interactables request through the cat's IAnchorRequester; that is the interception point).
- Q-8 is decided: **solo stays in v1** (record as D-026, below).
- Current state: PAX-021 committed; its Editor Play-mode and device tests are deferred and are
  folded into this ticket's acceptance tests.

---

STEP 0 — BEFORE CLAUDE CODE STARTS
`git status` clean and up to date with origin/main (PAX-021 committed and pushed).
Claude Code: check. If not true, stop and report.

---

CLAUDE — CODE

PRE-READ (report before editing; stop if anything contradicts this ticket)
a. SoloSwitchController: quote the switch method, how it holds/creates InactiveDriver and
   LocalHumanDriver instances, Initialize, and the Switched event.
b. ObserverContext.SetDriver and IObserverDriver (confirm Activate / FixedTick(int) / Deactivate).
c. How the SWITCH uGUI button and KeyboardSwitchInput are built and wired (component names, the
   ITouchReservedRegion implementation, selection clearing, which setup script creates them, and
   how TouchStickCatInput.reservedRegions gets the button). RECORD must mirror this exactly.
d. How the cat's facing is represented (SpriteRenderer.flipX, a motor property, or scale), and
   whether any rotation is written directly to the body in CatMotor2D.Step.
e. Everything that writes a cat's Rigidbody2D bodyType, position or velocity outside the motor
   (CatRespawn, FallResetVolume, setup scripts). If a respawn could act on a kinematic Echo cat,
   report what would happen.
f. RealityRoot: is there a local→world inverse of ToLocal?
g. Is keyboard `Z` bound anywhere?

Create — Parallax.Core (pure C#; Echo/ subfolder, namespace Parallax.Core)
- Core/Echo/EchoFrame.cs
  `public struct EchoFrame { public Vector2 Position; public float Rotation; public Vector2 GravityDirection; public bool FacingRight; }`
  Position is local to the Observer's RealityRoot. (Velocity, AnimState and InteractHeld from the
  §8.2 sketch are added when animation or hold interactions exist.)
- Core/Echo/EchoAnchorEvent.cs
  `public readonly struct EchoAnchorEvent { public readonly int TickOffset; public readonly AnchorId Anchor; public readonly float TargetValue; }`
  with a constructor. No origin or sequence is stored: playback issues fresh ones.
- Core/Echo/EchoRecording.cs
  Immutable: `ObserverId Observer`, `IReadOnlyList<EchoFrame> Frames`,
  `IReadOnlyList<EchoAnchorEvent> AnchorEvents` (sorted by TickOffset, stable).
- Core/Echo/EchoRecorder.cs
  - Constructor `(ObserverId observer, int maxFrames)`; throws if maxFrames < 1.
  - `int FrameCount`, `bool IsFull` (FrameCount == maxFrames).
  - `void AddFrame(in EchoFrame frame)`: ignored when full.
  - `void AddAnchorEvent(AnchorId anchor, float targetValue)`: ignored when full; otherwise
    TickOffset = FrameCount at the moment of the call. (A request made during tick N, before that
    tick's frame is captured, gets the same index as frame N.)
  - `EchoRecording Finish()`: returns a copy; the recorder can't be used afterwards (throws).
- Core/Echo/EchoPlayback.cs
  - Constructor `(EchoRecording recording)`; throws on null or zero frames.
  - `int Cursor` (0-based), `int FrameCount`, `bool IsHolding` (Cursor >= FrameCount).
  - `EchoFrame Advance(List<EchoAnchorEvent> due)`: clears `due`; if not holding, fills `due` with
    every event whose TickOffset == Cursor (recorded order), returns Frames[Cursor], then Cursor++.
    If holding, returns the last frame and leaves `due` empty.
  - `EchoFrame Current`: the frame most recently returned (frame 0 before the first Advance).
- Modify Core/Anchors/EventOrigins.cs: add `Echo(ObserverId id)` → EchoA / EchoB.

Create — Parallax.Gameplay (namespace Parallax.Gameplay.Echo)
- Gameplay/Echo/EchoConfig.cs — ScriptableObject: `float maxSeconds = 10f`;
  `int MaxFrames(float fixedDeltaTime) => Mathf.Max(1, Mathf.CeilToInt(maxSeconds / fixedDeltaTime))`.
- Gameplay/Echo/EchoSession.cs — MonoBehaviour on the Observers object.
  - Serialized: `ObserverSet observers`, `SoloSwitchController switchController`,
    `TransportHost transportHost`, `EchoConfig config`.
  - `enum EchoRecordState { Idle, Recording, Full, Unavailable }` and `State` property.
    Unavailable = the other Observer's driver is EchoReplay (one Echo at a time).
  - `void ToggleRecord()`:
    - Idle → start recording the **active** Observer (new EchoRecorder, subscribe to that cat's
      `CatInteractor.Requested`). No-op if Unavailable.
    - Recording or Full → discard (unsubscribe, drop the recorder) → Idle.
  - Frame capture: subscribed to `ObserverSet.Stepped`. While recording and not full, append one
    frame of the recording Observer: body position via RealityRoot.ToLocal, body rotation,
    `observer.Gravity.Direction`, facing (per pre-read d). When the recorder becomes full, stop
    capturing and unsubscribe from Requested; State = Full (the recording is kept for SWITCH).
  - `float RecordedSeconds` (FrameCount × fixedDeltaTime), for labels and the panel.
  - `bool TryCreateReplayDriver(ObserverId id, out IObserverDriver driver)`:
    true only if a recording for `id` exists with at least one frame. Finishes it, builds
    `new EchoReplayDriver(new EchoPlayback(recording), new AnchorRequester(transportHost.Transport,
    transportHost.Sequencer, EventOrigins.Echo(id)))`, clears the session → Idle.
    A recording with zero frames is discarded and returns false.
  - Never assigns drivers itself.
- Gameplay/Echo/EchoReplayDriver.cs — `IObserverDriver`, Kind = EchoReplay.
  - Constructor `(EchoPlayback playback, IAnchorRequester requester)`; null checks.
  - Activate(observer): cat body → Kinematic, zero linear and angular velocity; snap
    `body.position`/`body.rotation` to frame 0 (world via the RealityRoot); apply facing.
    Never calls the motor.
  - FixedTick(tick): `frame = playback.Advance(due)` (reused list); `MovePosition` /
    `MoveRotation` to the frame; apply facing; for each due event
    `requester.Request(e.Anchor, e.TargetValue)`. Holding: stays kinematic at the last frame.
  - Deactivate (cancel): body → Dynamic, zero velocities, position unchanged (the human resumes
    from where the Echo is); `observer.Gravity.SetTargetDirection(playback.Current.GravityDirection)`.
  - Read-only `Cursor`, `FrameCount`, `IsHolding` for the panel.
- Modify Gameplay/Interaction/CatInteractor.cs
  - Add `public event Action<AnchorId, float> Requested`.
  - The requester handed to interactables is a private forwarding wrapper: it calls the cached
    AnchorRequester, then raises Requested(anchor, target). Nothing else changes.
- Modify SoloSwitchController.cs
  - Serialized `EchoSession echoSession` (optional; null keeps today's behavior).
  - On SWITCH, for the Observer being left: if `echoSession != null &&
    echoSession.TryCreateReplayDriver(current, out var echo)` set that driver, else Inactive as
    today. The target Observer gets LocalHuman as today; if it was EchoReplay, SetDriver's
    Deactivate cancels the Echo. Camera handling and the Switched event are unchanged.
  - It stays the only code that calls SetDriver.
- Input for RECORD (mirror pre-read c):
  - `KeyboardEchoInput` (Z → `echoSession.ToggleRecord()`), on the DeviceInput rig.
  - A RECORD uGUI button left of SWITCH, same style, ASCII labels driven by EchoSession state:
    Idle "REC", Recording "REC 3.2" (seconds, one decimal), Full "REC FULL", Unavailable "REC --".
    Clears selection after click; implements ITouchReservedRegion the same way SWITCH does; added
    to TouchStickCatInput.reservedRegions.

Modify — Parallax.DebugTools
- DebugPanel.cs: serialized `EchoSession echoSession`. When open, add rows:
  `Echo: <State> <RecordedSeconds>s`, and for each Observer whose driver is EchoReplayDriver:
  `Replay <Id>: <Cursor>/<FrameCount> [holding]`. Grow `PanelRect` if needed; still one field.

Create — Parallax.Editor
- Editor/Setup/EchoSetup.cs, menu `PARALLAX/Setup/Echo (Sandbox)`, idempotent, compare-before-set
  (same helpers style as AnchorSetup): EchoConfig asset at `Assets/_Game/Data/EchoConfig.asset`
  (maxSeconds 10); EchoSession on Observers wired; `SoloSwitchController.echoSession`;
  KeyboardEchoInput on DeviceInput; RECORD button in HUD (mirroring how SWITCH was created) and
  its reserved-region wiring; `DebugPanel.echoSession`. Second run logs "EchoSetup: no changes."
  Runs RealityIsolationValidator and AnchorValidator at the end.

Create — Parallax.Tests.EditMode
- EchoTests.cs:
  - EventOrigins.Echo: A → EchoA, B → EchoB.
  - EchoRecorder: frames appended in order; maxFrames 3 → a 4th frame is ignored and IsFull;
    an event added before any frame has offset 0, after two frames offset 2; events after full
    are ignored; Finish returns the data and further use throws; maxFrames 0 throws.
  - EchoPlayback: returns frames in order; each event is due exactly on its offset tick; two
    events with the same offset keep recorded order; after the last frame it holds the last frame
    with no due events on any later Advance; `Current` is frame 0 before the first Advance and the
    last frame while holding; zero-frame recording throws.
  - Determinism: two EchoPlayback instances over the same recording produce identical frame and
    due-event sequences for FrameCount + 5 advances.
  - Integration (D-024): AnchorRegistry + LocalTransport + AnchorRequester(EchoA) sharing one
    sequencer. A recording with events (offset 0 → 1), (offset 2 → 0). Replay it once, pumping
    each tick: both commits Applied. Replay the same recording again with a new EchoPlayback and
    the same requester: both commits Applied again (never Duplicate).

Docs
- 02_ARCHITECTURE.md
  - §4.2: "As built (PAX-023)": the Observer being left becomes EchoReplay if EchoSession holds a
    recording for it, else Inactive; RECORD is unavailable while an Echo exists; RECORD while
    recording discards.
  - §8: "As built (PAX-023)": EchoFrame fields, frames captured on ObserverSet.Stepped, anchor
    events captured via CatInteractor.Requested as (TickOffset, Anchor, Target), playback issues
    them through an Echo-origin AnchorRequester with fresh sequences, cap stops capture but keeps
    the recording, hold final frame, cancel restores Dynamic and gravity from the current frame.
- 07_DECISIONS.md: append D-026 and D-027; mark Q-8 closed in the open-questions list
  (strikethrough, "Closed by D-026").
  ### D-026 · <date> · Accepted
  **Decision:** Solo stays in v1. Echo (PAX-023/024) and solo as a first-class mode are in scope.
  **Why:** Solo widens the audience beyond players with a partner, and the Echo machinery (state-based replay, re-issued requests) also serves Type B and Type C puzzles.
  **Consequence:** every hostile anchor must remain solvable against a ≤10 s Echo, which rules out traps that depend on the partner not knowing. SWITCH is a player-facing feature, not only a dev tool.
  **Resolves:** Q-8.
  ### D-027 · <date> · Accepted
  **Decision:** Echo v1 as built: (1) `EchoSession` records the active Observer after RECORD; frames are captured on `ObserverSet.Stepped`, anchor requests via `CatInteractor.Requested` as (TickOffset, Anchor, Target). (2) Playback is `EchoReplayDriver` (kinematic, never calls the motor), advanced by tick count; events are re-issued through an Echo-origin `AnchorRequester` with fresh sequences. (3) One Echo at a time: RECORD is unavailable while the other Observer is EchoReplay; RECORD while recording discards. (4) The 10 s cap stops capture but keeps the recording for SWITCH. (5) End of playback holds the last frame; cancel restores Dynamic, keeps position, and sets gravity from the current frame. (6) `SoloSwitchController` remains the only code that assigns drivers.
  **Why:** Tick-indexed state replay is exact and independent of physics; recording requests instead of interactions keeps the Echo working even if interactables change; a single interception point on the cat covers every interactable.
  **Supersedes:** 02_ARCHITECTURE §8.2 `EchoFrame` field list and `EchoAnchorEvent.Request` (refined; the removed fields return with animation/hold interactions).

---

YOU — UNITY EDITOR (walked through one step at a time)
1. Recompile; zero Console errors. EditMode tests all green.
2. Sandbox_Realities: run `PARALLAX/Setup/Echo (Sandbox)` twice (second run: no changes). Save.
3. Editor acceptance tests, including the deferred PAX-021 ones.
4. Build to the Pixel 8a; device tests.

DO NOT
- Add pressure plates, gates, overlap sensors or the HUD timeline (PAX-024).
- Record or replay control streams (Phase 5).
- Allow more than one Echo, looping, or Echo in co-op.
- Rewind anchor state when a replay starts (the Echo is a ghost; the world is not rewound).
- Change motor, camera, reality, anchor, transport or input-source code beyond what is listed.
- Add singletons, static mutable state, or runtime Find. Hand-edit .unity/.prefab/.asset/.meta.
- Commit. The Architect reviews first.

REQUIREMENTS
- Echo uses tick counts only, never Time.time.
- A replayed request goes through an Echo-origin requester with a fresh sequence, so a second
  replay of identical events still commits.
- The Echo cat is kinematic for the whole replay and hold; the motor is never stepped for it.
- SWITCH without a recording behaves exactly as before.
- RECORD and its key never produce a jump, stick move, or interaction.
- Only SoloSwitchController calls SetDriver.

ACCEPTANCE TEST

Editor — deferred PAX-021 checks first (latency 0 unless stated)
1. Panel shows Anchor#1 value 0.00 rev 0 before interacting.
2. As A: F away from the vine does nothing; F in the zone pulses, knot drops, rev 1.
3. Tab to B: elevator raised; jump onto it, then the ledge; the ledge is unreachable from the ground.
4. Inactive guard: A in the zone, Tab to B, F → rev unchanged.
5. Latency 300: F in the zone → pulse immediate, knot ~0.3 s later. Back to latency 0.

Editor — Echo
6. EditMode: all green.
7. SWITCH without REC: the left cat becomes Inactive, as before.
8. Basic Echo: as A, press Z (label counts up). Walk, jump, walk to the vine, F (up), wait ~2 s,
   F (down). Tab. Panel: A is EchoReplay, cursor counting. A's body jumped back to where
   recording started and repeats the path (turn on PiP to watch it).
9. Fresh sequences: during that replay the anchor table's rev rises by 2 more and
   LastCommitResult shows Applied, never Duplicate.
10. Type C sync: Tab to A, cancel; set up with B standing on the lowered elevator (Tab to B, walk
    on, Tab to A). Z, F (up), wait ~3 s, F (down), Tab. As B you ride the elevator up on the
    Echo's schedule and can jump to the ledge before it lowers.
11. Cancel mid-replay: Tab back to A while the Echo is moving → A continues from the Echo's
    current position under your control; events the Echo hadn't reached never fire.
12. Cap and hold: Z, move for more than 10 s (label shows REC FULL). Tab: the replay runs ~10 s,
    then A holds its last pose ("holding" in the panel). Tab back: A resumes from that pose.
13. One Echo at a time: while controlling B with A's Echo playing or holding, Z does nothing
    (label REC --).
14. Discard: as A, Z, move, Z again (label REC), Tab → A is Inactive.
15. Gravity: Z, rotate gravity with Q and move, Tab. The Echo reproduces the rotation. Tab back:
    A's gravity turns to match the Echo's current frame.
16. Latency 300: a recorded F replays with the elevator moving ~0.3 s after the Echo's press tick.
17. Movement, switching, gravity debug, debug anchor button unchanged. Zero warnings/errors.

Device (Pixel 8a) — includes deferred PAX-021 tests
18. Interact rect in the vine zone: immediate pulse, elevator in B raised after SWITCH; ride and
    reach the ledge with touch. Tapping interact never jumps or moves the stick.
19. Tapping REC never moves or jumps the cat; the label updates.
20. Test 10 with touch at latency 300.

DELIVERABLE (from Claude Code)
Standard CLAUDE.md output, plus:
- pre-read report (a–g) and any stop conditions hit;
- a tick walk-through: RECORD pressed between ticks N-1 and N, F read in tick N+50, SWITCH between
  ticks M and M+1 → the tick on which the Echo's frame 0, frame 50 and its request are applied,
  and the tick the commit lands at latency 0;
- grep proof, verbatim:
    grep -rn "Photon\|Fusion" Assets/_Game
    grep -rn "SetDriver" Assets/_Game --include=*.cs
    grep -rn "Time.time\|Time.realtimeSinceStartup" Assets/_Game/Core/Echo Assets/_Game/Gameplay/Echo
    grep -rn "Find" Assets/_Game/Core Assets/_Game/Gameplay
  (SetDriver may be called only from SoloSwitchController and ObserverBootstrap/Initialize, if that
  already did before this ticket; say which.)
- D-numbers used; new EditMode test count.
- Review file:
    { git status; git --no-pager diff; cat <every new .cs file>; } > ~/Desktop/pax023_review.txt 2>&1

DEFINITION OF DONE
Universal DoD, plus:
- You can explain in two sentences why the Echo replays state kinematically instead of replaying
  inputs, and why a replay must not re-send recorded sequence numbers.
- Deferred PAX-021 checks pass (Editor 1–5, device 18).
- Architect review passed before commit:
  `PAX-023: Echo recording and playback, RECORD, switch integration (D-026, D-027)`
