TASK: PAX-011
TITLE: Spawn point + fall reset volumes (instant respawn, no scene reload)
PHASE / GATE: Phase 2 (One ugly cat) / Gate 2

OBJECTIVE
When the cat leaves the playable area in any gravity direction, it is instantly returned to a spawn point
with gravity reset and motion cleared. No death screen, no scene reload.

CONTEXT
- Plan PAX-011; 02_ARCHITECTURE.md §11 (restore is instant: teleport, snap gravity, no reload),
  §8.5 / CLAUDE.md Echo ("Puzzle sensors use overlap queries, not trigger callbacks").
- CLAUDE.md Gravity: only GravityReceiver.SetTargetDirection changes gravity. D-006, D-012, D-019.
- Existing: GravityReceiver (Direction, target, FixedTick), CatMotor2D (IsGrounded, coyote/jump buffer
  timers, body alignment), GravityDebugControl (DebugTools; keeps its own `steps` counter),
  CatPlayerSetup.cs (menus: Configure Cat Player, Build Gravity Test Arena).
- Sandbox_Player: Ground (0,-4) scale (18,1); GravityArena with Wall_Left (-8.5,0), Wall_Right (8.5,0),
  Ceiling (0,4); scene Cat_Player at about (0,-3.1).

CLAUDE — CODE
Allowed files (nothing else):

1. MODIFY Assets/_Game/Gameplay/Player/GravityReceiver.cs
   - Add `public Vector2 TargetDirection => target;`
   - Extend the single entry point: `SetTargetDirection(Vector2 dir, bool snap = false)`.
     When snap is true, Direction is set to the target immediately. Existing callers unchanged.

2. MODIFY Assets/_Game/Gameplay/Player/CatMotor2D.cs
   - Add `public void ResetMotion()`: zero linearVelocity and angularVelocity, clear coyote and
     jump-buffer timers, clear the stored command, and set body rotation to match gravity.Direction.

3. CREATE Assets/_Game/Gameplay/Player/CatRespawn.cs   (namespace Parallax.Gameplay.Player)
   - `[RequireComponent(typeof(CatMotor2D), typeof(GravityReceiver))]`.
   - `public void RespawnAt(Vector2 position, Vector2 gravityDirection)`: snap gravity via
     `SetTargetDirection(gravityDirection, snap: true)`, teleport the Rigidbody2D (body.position and
     transform.position), call motor.ResetMotion(). Log one line naming the cat.
   - Ignore repeated calls within the same FixedUpdate step (a cat touching two volumes respawns once).

4. CREATE Assets/_Game/Gameplay/Checkpoints/SpawnPoint.cs   (namespace Parallax.Gameplay.Checkpoints)
   - Serialized `Vector2 gravityDirection = Vector2.down`. Public `Position` (world) and `GravityDirection`.
   - OnDrawGizmos: small circle plus an arrow showing gravityDirection.

5. CREATE Assets/_Game/Gameplay/Checkpoints/FallResetVolume.cs   (namespace Parallax.Gameplay.Checkpoints)
   - `[RequireComponent(typeof(BoxCollider2D))]`; Reset()/Awake set the collider to isTrigger = true
     (shape only). Serialized `SpawnPoint spawn`, `LayerMask catMask` (default Everything).
   - Each FixedUpdate: `collider.Overlap(filter, results)` with a preallocated buffer and
     useTriggers = false. For each hit, find CatRespawn on `attachedRigidbody` and call
     RespawnAt(spawn.Position, spawn.GravityDirection). NO OnTriggerEnter2D/Stay2D callbacks.
   - Missing spawn → log an error once and disable the component.

6. MODIFY Assets/_Game/DebugTools/GravityDebugControl.cs
   - Remove the internal `steps` counter. Q/E rotate from `receiver.TargetDirection`
     (GravityMath.Rotate90(target, +1 / -1)); R sets down. Keeps working correctly after a respawn.

7. MODIFY Assets/_Game/Editor/CatPlayerSetup.cs
   - Configure Cat Player: also add CatRespawn to the prefab root if missing (idempotent, logged).
   - ADD menu `PARALLAX/Setup/Build Fall Reset Test` (active scene; idempotent via a root "FallResetTest"):
     a. Requires "Ground" and "GravityArena/Wall_Right"; otherwise log an error and stop.
     b. Create root "FallResetTest" containing "Spawn_Main" (SpawnPoint) at (0,-3.1), gravity down.
     c. Create four FallResetVolume children, each with a BoxCollider2D, wired to Spawn_Main:
        KillZone_Bottom center (0,-9) size (40,4); KillZone_Top (0,9) size (40,4);
        KillZone_Left (-14,0) size (4,22); KillZone_Right (14,0) size (4,22).
     d. Deactivate GravityArena/Wall_Right (SetActive false) so there is an exit to fall through.
     e. Undo registration, mark scene dirty, do not save. Log a summary.

DO NOT
- Use OnTriggerEnter2D/Stay2D, singletons, FindObjectOfType/FindAnyObjectByType at runtime, or scene reloads.
- Change gravity other than through SetTargetDirection. Use Physics2D.gravity or Time.time.
- Add checkpoints/snapshots, death UI, fades, camera, touch input, layers, Observers, networking.
- Touch .unity, .prefab, .asset, .meta, ProjectSettings/, Packages/, Docs/ by hand. Run git commands.
- Create files outside the allowed list.

REQUIREMENTS
- Leaving the area downward, sideways (debug gravity) or upward returns the cat to Spawn_Main within one physics step.
- After respawn: gravity is down and settled (no turning animation), velocity zero, no stored jump,
  body upright, walking/jumping work immediately.
- Detection uses overlap queries only.
- Debug gravity keys behave correctly after a respawn.

YOU — UNITY EDITOR (after Architect review)
1. Compile; no red errors.
2. Run PARALLAX → Setup → Configure Cat Player (expect: added CatRespawn). Run again: already configured.
3. In Sandbox_Player run PARALLAX → Setup → Build Fall Reset Test, save the scene. Run again: already built.
4. Test Runner → EditMode → Run All: still green.

ACCEPTANCE TEST (Editor only)
1. Play. Walk right off the ground's end (Wall_Right is gone): the cat falls and reappears at the spawn, standing, gravity down.
2. Press E until gravity points right: the cat falls out through the opening and respawns upright with normal gravity.
3. After respawn, Q/E/R work as before (first E press turns exactly 90° from down).
4. Jump right before hitting a kill zone: no buffered jump fires after respawn.
5. Repeat 10 times quickly: no errors, no duplicate respawn logs per fall.

DELIVERABLE (from Claude)
Per CLAUDE.md "Required output"; state clearly what was and was not observed in Unity.
