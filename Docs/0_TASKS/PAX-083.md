# PAX-083 · Cleanup: every open small item after KIT-4

**Status:** Approved for implementation.
**Phase 1 size: Lite.** There's no new design. Phase 1 locates each item and confirms the file list. The CLAUDE.md "Ticket phases and agent budgets" rules apply: at most 6 questions, and a short trace.

**Phase:** Solo v1, Phase B. It's the last ticket before PAX-058 and Phase D.
**Depends on:**
- PAX-076 committed, with **D-083 in `07_DECISIONS.md`**;
- the EditMode baseline at that commit: 675, all green (§12 R1).

**Decisions it touches (none change):**
- D-079 (7)–(8): the redundant checks, and L001/L003 `Retreat` left for KIT-3b;
- D-080: betrayal outcomes (Dies / Recovers);
- D-083: KIT-4, including its unproven tests and the reviewer's nits.
- **No new decision is expected.** If item 9 needs a runtime change, it stops (§4), and a later ticket takes D-085.

**Menus are the developer's job.** Don't run setup menus or modify scenes through MCP.

---

## 1. Why

- Small leftovers have piled up across PAX-074 to PAX-076: reviewer nits, tests that leave scenes dirty, console noise nobody has written down, a confusing message, and tests never seen failing.
- Phase D starts adding 50 levels. Every leftover makes it harder to tell a real problem from old noise.
- **This ticket changes nothing the player sees.** It changes tests, messages, docs and route data only.

## 2. What to do

Every item is independent. If one gets stuck, report it and move on (§4).

**A. Reviewer nits**
1. **The 6 PAX-075 reviewer nits.** Find them in the PAX-075 review output (`pax075_review.txt` or the reviewer's report in git history). Known example: the scene-hygiene fingerprint compares delegates by hash code. Fix each one that stays inside §5's rules.
2. **The 2 KIT-4 reviewer nits:**
   - the in-view camera test also asserts that the room is in follow mode;
   - `OnScreenLead` guards against a replay with fewer ticks than the kill tick, with a clear failure message instead of an index error.

**B. Test hygiene**
3. **`LevelSelectLayoutTests.BuildRowTemplate_…` dirties the active scene.** Make it restore the Test Runner's scene clean (CLAUDE.md's rule, PAX-075 R21).
4. **The test that marks `Level_004` dirty** (PAX-075 R21, left as a known limit). Find it and make it leave the scene clean. Nobody should be asked to save a level after a test run.

**C. Console noise**
5. **Known-noise list in CLAUDE.md.** For each line, find the test that prints it and confirm it's deliberate or harmless. Then either add it to the list or fix its source:
   - `ObserverSet 'Observers' has no observerB assigned.`
   - `RoomManager: cat left room 0 bounds at …` (every position variant);
   - `LevelCameraFollow: 'Camera_A' has an unbaked (zero) camera frame; skipping until it's regenerated.` Say whether it appeared before PAX-076.
   - the `mcp.log` import warning is already listed (D-083 R8). Check that it is.

**D. Messages and route data**
6. **The chain validator's "cross-room" message.** A non-trap source in the same room currently gets "chain source must be a trap in this room; cross-room names cannot resolve by construction". Split it into two messages, one per case, each saying only what applies.
7. **L001 `Retreat` becomes a Recovers betrayal** (D-080's `Betrayal.Recovers`). Also check L003 `Retreat`, which D-079 (8) lists the same way. Add a Recovers route for each one that passes.
   - Route data only (`L001Routes.cs`, `L003Routes.cs`). No layout change.
   - Existing route results stay identical.

**E. Docs**
8. **The last Trap Lab room turns its hazards off once completed.** Write that down where the Trap Lab is described (`02_ARCHITECTURE.md`, or CLAUDE.md if that's where Trap Lab behaviour lives), so nobody takes it for a bug again.

**F. The open question from PAX-082**
9. **"Jump detection above 9 u/s" (PAX-082 R10).**
   - Is it a test threshold or runtime code? Give file:line.
   - What's the measured launch speed at the first jump tick with the D-082 motor? The continuous value is 9.80 u/s.
   - What margin is left?
   - **Report only.** Don't change runtime code (§4).

**G. Unproven tests (see each fail once)**
10. Take each unproven test below, make a one-line temporary change so that it fails, quote the failure, then revert:
    - PAX-080: the reset test and the look-match test;
    - KIT-4: the pure-step equivalence cases, the recorded-position test, room 5's pinned results, and the bait attempt.

    A test that can't be made to fail with a small temporary change stays "unproven". Say why.

**H. Scene sync and the Trap Lab record (§11 P3, P4)**
11. **The saved-scene sync gap.** A test opens the saved `Sandbox_TrapLab.unity` and runs
    `TrapLabSetup.DifferencesFromLayout` on every room. Seen red against the 87b9fad scene. It restores the Test
    Runner's scene (CLAUDE.md). Also: if the saved `Level_00N.unity` scenes get no full comparison against their
    layouts today, add the same kind of test for them when the builder offers a comparison like
    `DifferencesFromLayout`; if it doesn't, report the gap as an open item.
12. **A developer play of Trap Lab rooms 0–4** (§9). Rooms 0–2 have never been played at their refit, and rooms 3–4
    never at their PAX-082 layout.
13. **Skipped, outcome (a), §12 R2: D-083 (10) already records it.** ~~Record the rooms 0–2 refit in D-083.~~ Append one line to D-083: "Also refit Trap Lab rooms 0–2 to D-082's cat:
    every trigger spans the cat's band (D-074), ThinPlatform y 0.85, Crusher and FixedPillar 1 × 1, and PeriodicSpikes
    has a 6-tick reveal (D-057). Covered by TrapLabRefitTests."

## 3. Phase 1 (Lite: at most 6 questions, a short trace)

1. **The PAX-075 nits.** List all 6, with where you found them. For any you can't find, say where you looked.
2. **Dirty scenes.** Which test dirties `Level_004`? What does `BuildRowTemplate_…` touch? Give file:line.
3. **Noise sources.** For each line in item 5, name the test or code that prints it, and say whether it's deliberate.
4. **`Retreat`.** For L001 and L003: what does each `Retreat` reveal, and does a Recovers route look possible (the cat survives, the room completes, `revealedBy` visibly changes first)?
5. **Item 9.** File:line, and test or runtime.
6. **Files.** The allowed-file check against §7. Confirm that PAX-083 is a free ticket number in `08_V1_ROADMAP.md`.

Measured in Phase 2, not Phase 1: the item-9 launch speed, the Recovers results, and every "seen red".

## 4. Outcomes, decided now

- **(a) An item is done within §5's rules.** Done; no stop.
- **(b) An item would need runtime code, a layout change, or a change to an existing route result or pin.** Skip that item and report why, with the smallest fix. No stop.
- **(c) A PAX-075 nit can't be found.** Report where you looked, and skip it. No stop.
- **(d) A `Retreat` route doesn't pass as Recovers** (the cat dies, the room doesn't complete, or nothing visibly changes). Don't change the layout. Leave that one unclaimed, and record it in the report. No stop.
- **(e) Item 9 is runtime code, and the launch speed at the first jump tick is under 9.3 u/s** (a margin under 0.3). **Stop.** Report the numbers and propose a fix. A runtime change needs its own ruling and D-085.
- **(f) Any existing test goes red and stays red after your change is reverted.** Stop.

## 5. Rules

- **No runtime code changes.** Gameplay, Core and every scene stay as they are.
- **No geometry changes.** Layouts are unchanged, and every existing route result and pin is unchanged.
- **Tests:**
  - existing tests change only for items 1–4, and only in how they set up and restore scenes, never in what they assert;
  - new tests are deterministic, and tests that open scenes restore the Test Runner's scene (CLAUDE.md).
- Messages change only for item 6 (and item 2's new guard message).
- `NoFullSpeedTakeoff` and `RoomStepper` stay (PAX-082 R6).

## 6. Tests

Mark every test "seen red" (quote the failure) or "unproven".

- Item 2: the follow-mode assertion; the short-replay guard (seen red on a truncated replay).
- Items 3–4: after each fixed test, no scene is dirty (seen red before the fix).
- Item 6: one test per new message (each seen red).
- Item 7: each new Recovers route passes (seen red with its `revealedBy` pointed at an element that never changes).
- Item 10: the seen-red list, one line each.
- **Total:** 675 plus the new tests, all green (§12 R1). Report the added test time.

## 7. Allowed files

- The test files named by the PAX-075 nits, and by items 2–4 and 10.
- `LevelLayoutValidator.Camera.cs` (item 2) and the chain validator file (item 6): messages and guards only.
- `L001Routes.cs`, `L003Routes.cs` (item 7).
- Test-only helpers, if an item needs one.
- New tests, including item 11's saved-scene sync test(s) (test files only).
- `Editor/Routes/RouteSession.cs` and `Editor/Routes/RouteReplay.cs`: PAX-075 nits 1, 2 and 4 only (§12 R6).
- `Level_00N.unity`: only if item 11 finds a stale level, and then changed only by the developer's Rebuild All Levels run (§12 R5 (b)).
- **Docs:**
  - `CLAUDE.md`: the known-noise list (item 5), and the Trap Lab note if it belongs there (item 8);
  - `02_ARCHITECTURE.md`: the Trap Lab note (item 8);
  - `08_V1_ROADMAP.md`: the PAX-083 row, done;
  - `07_DECISIONS.md`: **only** if item 7 adds Recovers routes. Then one line appended to D-080: "PAX-083: L001 `Retreat` [and L003 `Retreat`] declared as Recovers."
- Any other file needs a ruling first.

## 8. Out of scope

- The frame rate: Unity's mobile default is 30 fps, and nothing sets it. That's a feel decision for later (Phase H or its own ticket), not cleanup.
- The `HostColor` side-wall nit (PAX-074): still unneeded.
- The co-op sandboxes affected by the new jump height: co-op is frozen.
- Removing `NoFullSpeedTakeoff` or `RoomStepper`.
- Any runtime fix from item 9 (§4 (e)).

## 9. YOU: Unity Editor

No menus. No scene should change.

1. **Test Runner: Run All (EditMode).** All green, at the total from §6.
2. Run `git status`. You should see only the §7 files, with no `.unity` file and nothing under `ProjectSettings/`.
3. Optional: play L001 and walk back from the `Retreat` spot, if item 7 claimed it. Does it feel like a fair "gotcha" you recover from?
4. **Item 12:** open `Sandbox_TrapLab` and play rooms 0–4 once each. Report whether the arrows and the betrayals feel
   fair. No menu, no save.

## 10. Required output

As `CLAUDE.md`, plus:
- the Phase 1 trace (§3.1–3.6) with file:line references;
- a table with one row per item: done / skipped (outcome letter) / not found;
- item 9's numbers;
- the known-noise lines added, each with its source;
- the test names, each marked "seen red" or "unproven";
- the added test time;
- `pax083_review.txt`;
- one `pax-reviewer` loop, at most two rounds;
- the `git add` commands (one commit).

Commit: `PAX-083: cleanup after KIT-4`.

## 11. Pre-flight rulings (P1–P3)

**P1 · 1a07959: accept it as it is (A).** It counts as the PAX-076 (KIT-4) commit plus the PAX-083 ticket, under the
wrong message. Don't rewrite it. Checks 1 (the 49 files match `pax076_review.txt`; nothing under `Assets/_Recovery`,
no `Level_00N.unity`, no `Sandbox_Realities.unity`, nothing under `ProjectSettings/`) and 3 (D-083 is the R10 version;
no "tell ≥ 7" in `07_DECISIONS.md` or `CLAUDE.md`) passed.

**P2 · The Trap Lab scene: P2-A, with conditions.** Stopping was right, and the analysis is accepted.
- What happened: the scene is generated by the menu from `TrapLabLayout.cs`. The code in 1a07959 matches the review
  byte for byte. The reviewed scene was stale: rooms 0–4 had never been rebuilt after PAX-082's refit. The developer's
  KIT-4 menu run rebuilt every room, so the committed scene is the reviewed code, built. A reviewer pass on generated
  YAML would add nothing that a scene-against-layout test can't prove better.
- Conditions. Stop if any one fails:
  1. `git diff 4f5ba10 1a07959 -- TrapLabLayout.cs` touches no line belonging to rooms 0–4, apart from additions for
     room 5. The trigger sizes such as SourceSpikes (5, 3.5)/(0.5, 7) must then already be in the file at 4f5ba10.
  2. For each value in the room 0–4 list, the committed scene equals `TrapLabLayout.cs` at 1a07959. A spot check of one
     element per room is enough, if item 3 covers the rest.
  3. Explain why `TrapLabSyncTests` / `TrapLabRefitTests` were green at the 624 and 666 baselines while the committed
     scene was stale: either the working-tree scene during those runs wasn't the committed one (say how), or the sync
     tests don't check these fields. Say which, and name the fields they do check.
- Then run the rest of pre-flight: `refresh_unity`, the console, and EditMode 666/666 green with the time. The sync and
  refit tests must be green.
- D-083 (7) ("Trap Lab 3 is unchanged") stays as it is. Record in the PAX-083 report: "1a07959 brought
  Sandbox_TrapLab.unity rooms 0–4 up to the PAX-082 layout; the PAX-082 commit had shipped them stale."
- **Check result (2026-09-24): condition 1 failed, stopped.** The diff modifies 7 lines in rooms 0–2 (PAX-076's own
  refit of rooms 0–2: ThinPlatform, the SourceSpikes/SlidingSpikes/Crusher/RearmBlock triggers, Crusher, FixedPillar,
  PeriodicSpikes' `revealDelayTicks: 6`); rooms 3–4 are untouched. Awaiting a ruling.

**P3 · Two items added to PAX-083 (§2):**
- **Item 11, the scene sync gap.** If P2.3 shows that the sync tests don't check trap settings or trigger and element
  sizes, extend them so that a stale Trap Lab or level scene fails. Seen red: run the extended test against the
  pre-rebuild blob (87b9fad's scene); it must fail. Test files only, added to §7. If the tests were already complete,
  and P2.3's answer is a working-tree difference, write that down instead and skip the item.
- **Item 12, a developer play of Trap Lab 3 and 4** (added to §9). They've never been played at their PAX-082 layout:
  the PAX-082 play ran on the stale scene. Play each once and report whether the arrows and the betrayals feel fair.

## 11 · amendment 1 · Pre-flight ruling P4

**P4 · The scene: accept it (P4-A), with the corrections below.** Stopping was right again, and the trace is accepted.
- What 1a07959 did to the Trap Lab scene: it brought rooms 3–4 up to the PAX-082 layout, built PAX-076's own refit of
  rooms 0–2, and added room 5. The committed scene matches `TrapLabLayout.cs` (condition 2's spot checks). Report
  sentence, corrected: "1a07959 brought rooms 3–4 up to the PAX-082 layout, and built PAX-076's rooms 0–2 refit, which
  the reviewed scene lacked."
- A scope note on PAX-076. The rooms 0–2 refit went beyond PAX-076 §7 ("room 5, and any (b) fixes to rooms 0–4").
  Rooms 0–2 declare no routes, so the camera rule couldn't have caused it. The Phase 2 report also said
  `TrapLabLayout.cs` only added lines against HEAD, which was wrong. The code was reviewed and is covered by
  `TrapLabRefitTests`, so it stays. It gets recorded instead:
  - Item 13 (new, docs): append one line to D-083 in `07_DECISIONS.md`: "Also refit Trap Lab rooms 0–2 to D-082's
    cat: every trigger spans the cat's band (D-074), ThinPlatform y 0.85, Crusher and FixedPillar 1 × 1, and
    PeriodicSpikes has a 6-tick reveal (D-057). Covered by TrapLabRefitTests." Add `07_DECISIONS.md` (this line only)
    to §7.
- Item 11, confirmed as needed: a test opens the saved `Sandbox_TrapLab.unity` and runs
  `TrapLabSetup.DifferencesFromLayout` on every room. It's seen red against the 87b9fad scene, and it restores the
  Test Runner's scene (CLAUDE.md). In Phase 1, also say whether the saved `Level_00N.unity` scenes get a full
  comparison against their layouts today (`LevelSceneTimingTests` checks timing only). If they don't, add the same kind
  of test for them, if the builder offers a comparison like `DifferencesFromLayout`. If it doesn't, report the gap, and
  it becomes an open item.
- Item 12, widened: the developer plays Trap Lab rooms 0–4 once each. Rooms 0–2 have never been played at their
  refit, and rooms 3–4 never at their PAX-082 layout.
- The PAX-076 menu change (it now rebuilds every room, not only missing ones) is accepted. It's the reason the scene
  is now right.
- Now add items 11–13 to §2, §7 and §9. Then run the rest of pre-flight: `refresh_unity`, the console, and EditMode
  666/666 with the time. Then do Phase 1 (Lite) and stop after it.
- **Correction (§12 R2):** the rooms 0–2 refit was the developer's request during PAX-076 §9, and D-083 (10) records
  it. P4's scope note is withdrawn.

## 12. Phase 1 rulings (R1–R8)

The trace was good. Phase 2 is approved under these rulings.

**R1 · The baseline is 675.** Correct the "Depends on" line and §6 to 675. The docs (D-083 (11), the roadmap) are
right. The ticket's 666 was taken from before R12's tests.

**R2 · Item 13: skip it (outcome (a)). D-083 (10) already records the rooms 0–2 refit.** Add one correction line under
§11 amendment 1: "Correction: the rooms 0–2 refit was the developer's request during PAX-076 §9, and D-083 (10)
records it. P4's scope note is withdrawn." Remove `07_DECISIONS.md`'s D-083 line from §7. The D-080 line for item 7
stays.

**R3 · Items 3 and 4: one fix, one seen-red.**
- Fix both `BuildRowTemplate` tests: build under a scene the test creates, then restore through
  `RouteSession.RecreateUntitledScene`.
- Seen red: after the test and before the fix, the active scene is dirty.
- Item 4 closes as covered by the same fix, because the `Level_004` case was this test acting on the scene that leaked
  before R21.

**R4 · Retreat:**
- L001: declare a Recovers route, as traced (hesitate, let Block_A land and the door retreat, then walk to x 31). Seen
  red as §6 says. Append the D-080 line.
- L003: (a). Leave it unclaimed, and record "the retreat fires on the solution itself; it's part of the solution, not a
  betrayal" in the report and on the D-080 line.

**R5 · Item 11, the scene checks:**
- (a) Seen red without touching a .unity file. Make a one-line temporary change to `TrapLabLayout.cs` (for example one
  `unitsPerTick`), and the saved-scene test must fail naming that element. Then revert. Do the same for one level
  layout. Don't swap or hand-edit any .unity file.
- (b) If a `Level_00N.unity` is stale today (the new level test is red on a clean tree):
  - that's a real finding, not a stop;
  - report each difference per level: object path, field, scene value and layout value;
  - the developer runs Rebuild All Levels in §9;
  - the `Level_00N.unity` files that changed are added to §7, as changed only by that menu run;
  - you check them with an object-path diff after the rebuild. The only differences allowed are those in your report,
    plus fileID churn;
  - the developer plays every level that changed (added to §9);
  - the test is expected red until the rebuild, like the thresholds asset in PAX-076.
- The test never saves, and it restores through `RecreateUntitledScene`, as `LevelSceneTimingTests.cs:94` does.

**R6 · Nits 1, 2 and 4: `Editor/Routes/RouteSession.cs` and `Editor/Routes/RouteReplay.cs` are added to §7,** for
those nits only.
- Nit 1, the fingerprint: compare delegates by target and method, not by hash, and include the recreated untitled
  scene's roots. Seen red on a case the old fingerprint missed, if you can build one cheaply; otherwise it's "unproven".
- Nit 2: a comment only.
- Nit 4: unsubscribe `rig.Death.Died`. It's "unproven" (no behaviour test).
- All determinism and route tests stay green, with unchanged results.
- Nits 3, 5 and 6: no change, as proposed. Record them as closed.

**R7 · Item 2, the short-replay guard.** It's a real bug: a short replay silently overstated the lead.
- The guard fails with a clear message when `records.Count < end`, and also when the replay is empty.
- Before adding it, check that no existing camera-rule result came from a short replay. Log `records.Count` against
  `end` for every reveal in L001–L004 and Trap Lab 3–5.
- If any was short, stop: a pinned KIT-4 lead may be wrong.

**R8 · The rest is accepted as traced:**
- Item 5: add the three noise lines, with their sources, to CLAUDE.md's list (the ObserverSet, the RoomManager bounds,
  and the LevelCameraFollow zero-frame line for test runs).
- Item 6: split the message in `TrapLayoutValidator.cs:20`.
- Item 8: the note in `02_ARCHITECTURE.md`.
- Item 9: it's a test threshold, so outcome (e) can't apply. Still measure the launch speed in Phase 2 and report the
  margin.
- Items 10–12 as written.
- Add a PAX-083 row to `08_V1_ROADMAP.md` in place of "the cleanup ticket". No decision number is used; D-085 stays
  free.
- `claude/PAX-083_preflight_rulings.md` lives in the Architect's project, not the repo. Nothing to do.

Don't stop again unless: R7 finds a short replay, a test stays red after its temporary change is reverted, or any
existing route result or pin changes.

**R9 · The Recovers pin (Phase 2 stop): ruling A.** `ShippedRouteResultsTests.cs:42` pinned "L001-L004 declare no
Recovers betrayal", written before item 7. It may change, on three conditions:
- a pin per level, not a total count: L001 exactly 1 Recovers; L002, L003 and L004 each still 0;
- L001's details pinned too: revealedBy `Door`, Door visible at t252, complete at t345;
- the failure message and D-080 match: D-080's line says L001 recovers, and that revealedBy is `Door` on purpose
  (`DoorRetreatTrap` moves the door, not its own element).

This is the only assertion in an existing test changed by ruling in PAX-083. Reviewer checks: nothing else pins the
total replay count (L001 went from 30 to 31 replays), and the time increase is just that replay; the nit 1 tick-source
test has never failed and is listed as a known gap in `pax083_review.txt`, not as covered.
