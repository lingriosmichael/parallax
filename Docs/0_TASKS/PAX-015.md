TASK: PAX-015 (absorbs PAX-016)
TITLE: Two realities: roots, layers, second cat, per-Observer cameras, per-reality lighting
PHASE / GATE: Phase 3 / toward Gate 3

OBJECTIVE
One sandbox scene holds Reality A and Reality B at a fixed offset, each with its own
geometry, physics layer, sorting layers, global light, cat, and camera. Cat A collides only
with Reality A and Cat B only with Reality B. A dev-only view toggle shows either reality:
A reads warm, B reads cold.

CONTEXT
- 02_ARCHITECTURE.md §3.1 (Reality roots), §3.2 (Cameras), §4 (ObserverContext), §6 (ground detection).
- D-004, D-005, D-020/D-021 (world-aligned cameras, no rotation), D-022 (ObserverSet tick, device-level input).
- Current state after PAX-014: Sandbox_Player contains Cat_Player (prefab instance, no input),
  GravityArena (plain colliders), spawn point(s), fall volume(s), one camera with CatCameraFollow,
  DeviceInput, Observers (ObserverSet, ObserverBootstrap, ObserverDriverDebugToggle), Observer_A.
  ObserverBootstrap already gives B an InactiveDriver if B exists.
- Merged: plan PAX-016 (cameras, sorting layers, lights) is part of this ticket. Its parts can't
  be tested separately.

---

STEP 0 — YOU, BEFORE CLAUDE CODE STARTS

1. PAX-014 is committed and pushed, and Editor tests 1–9 and device tests 10–14 passed.
   If anything failed or was skipped, stop and report first.
2. Project Settings → Tags and Layers → **Layers**: add two User Layers, `RealityA` and `RealityB`.
3. Project Settings → Tags and Layers → **Sorting Layers**: keep `Default` at the top of the
   list, then add in exactly this order:
   `A_Background, A_Middle, A_Gameplay, A_Foreground, B_Background, B_Middle, B_Gameplay, B_Foreground, UI`
   (skip `UI` if it already exists; move it to the bottom).
4. Project Settings → Physics 2D → Layer Collision Matrix: **uncheck RealityA × RealityB**.
   Leave everything else as is.
5. In the Project window, duplicate `Sandbox_Player` (Ctrl+D) and rename the copy to
   `Sandbox_Realities`. From now on it is the working sandbox. Leave `Sandbox_Player` frozen as
   the Gate 2 reference; do not delete it.
6. Commit: `PAX-015 prep: reality layers, sorting layers, collision matrix, Sandbox_Realities`.

(Layers and sorting layers are set by hand because editing TagManager from script is fragile.
The setup script below validates them and stops with an exact list of what's missing.)

---

CLAUDE — CODE

Before editing, read and report on the following. **Stop and report** if any of them
contradicts this ticket.
- Every Physics2D query, ContactFilter2D, LayerMask, and trigger callback under `_Game/`
  (CatMotor2D, CatRespawn, FallResetVolume, SpawnPoint, CameraMath/CatCameraFollow, GravityDebugControl).
- Whether FallResetVolume, CatRespawn, or CameraMath use **world-space thresholds or bounds**
  (e.g. "y < -20"). Those break at a 1000-unit offset and must become reality-relative.
- How CatRespawn finds its SpawnPoint, and how FallResetVolume finds its cat and spawn
  (serialized refs vs lookups).
- The serialized property name for a Light2D's target sorting layers in the installed URP
  package source (Library/PackageCache). Expected: `m_ApplyToSortingLayers`; verify it.

Create — Parallax.Core
- Assets/_Game/Core/Reality/RealitySpace.cs — pure static.
  - `public static readonly Vector2 OffsetB = new Vector2(0f, 1000f);`, the only place the
    offset is defined.
  - `Vector2 Origin(ObserverId reality)`: A → zero, B → OffsetB.
  - `Vector2 MapTo(ObserverId from, ObserverId to, Vector2 worldPos)` → `worldPos - Origin(from) + Origin(to)`.
  - `string PhysicsLayerName(ObserverId)` → "RealityA"/"RealityB".
  - `string SortingLayerName(ObserverId, SortingBand)` → e.g. "A_Gameplay".
  - `enum SortingBand { Background, Middle, Gameplay, Foreground }`.
  Reality identity reuses ObserverId: Reality A belongs to Observer A, always (D-004/D-005).
  No separate RealityId type.

Create — Parallax.Gameplay
- Assets/_Game/Gameplay/Reality/RealityRoot.cs — MonoBehaviour.
  - Serialized `id` (ObserverId).
  - Exposes `Id`, `LayerMask PhysicsMask` (resolved once via LayerMask.NameToLayer; log an error
    if the layer is missing), `Vector2 ToLocal(Vector2 world)`, `Vector2 ToWorld(Vector2 local)`,
    and `Vector2 MapTo(RealityRoot other, Vector2 worldPos)` (delegates to RealitySpace).
  - Awake: log an error with context if `transform.position` ≠ `RealitySpace.Origin(id)`,
    or if rotation/scale are not identity.
- Assets/_Game/DebugTools/RealityViewDebugToggle.cs — dev-only.
  - V key plus an OnGUI button, placed below ObserverDriverDebugToggle's button (upper-left).
  - Toggles which Observer's `Camera` component is enabled (A or B); exactly one at a time.
  - **Does not change drivers.** Label: "View: A|B — driving: <A's Driver.Kind>".
  - Toggle the Camera **component** only, never the GameObject, so CatCameraFollow keeps
    tracking while the camera is hidden.
  - Replaced by the real SWITCH in PAX-017.

Create — Parallax.Editor
- Assets/_Game/Editor/Setup/RealitySetup.cs — menu `PARALLAX/Setup/Realities (Sandbox)`.
  Must be idempotent. Operates on the active scene. After the roots exist, every lookup is
  root-relative (`transform.Find`, component queries under a root), never `GameObject.Find`,
  because A and B contain objects with the same names. Steps:
  0. Validate: both physics layers exist; all nine sorting layers exist; Physics2D ignores
     RealityA×RealityB. If anything is missing, log the exact missing items and **stop without
     changing the scene**.
  1. Ensure `RealityRoot_A` at the origin and `RealityRoot_B` at `RealitySpace.OffsetB`, each with
     RealityRoot (id set) and child folders `Geometry`, `Lights`.
  2. Populate A (only if not already done): move `Cat_Player` under RealityRoot_A and rename it
     `Cat_A`. Move `GravityArena` under `RealityRoot_A/Geometry`. Move every SpawnPoint and every
     FallResetVolume under RealityRoot_A. Keep world positions (A is at the origin, so local ==
     world). Log a warning for any remaining scene-root object with a Collider2D or Renderer that
     wasn't moved. Don't move it.
  3. For each root: set the GameObject layer recursively to its reality layer. Put every
     SpriteRenderer whose sorting layer isn't already one of that reality's layers onto
     `X_Gameplay`.
  4. Populate B (only if `RealityRoot_B/Geometry/GravityArena` is absent): duplicate A's
     GravityArena, spawn point(s), and fall volume(s) into RealityRoot_B at the **same local
     positions**.
     - **Rewire every object reference in the B copies that points into RealityRoot_A to the
       corresponding B object** (spawn refs in particular). A reference with no B counterpart is
       an error, logged by path.
     - Add one **B-only pillar**: duplicate a floor-level platform collider, placed at local
       (spawn.x + 3, floor top), tall enough (≥ 2× cat height) that walking right from Spawn_B
       is blocked. Name it `Pillar_BOnly`.
     - Tint B geometry sprites cold (e.g. #6FA8DC). Leave A's tint unchanged.
  5. Cat_B: `PrefabUtility.InstantiatePrefab` of Cat_A's prefab asset (resolve it via
     GetPrefabAssetPathOfNearestInstanceRoot; keep the prefab link) under RealityRoot_B at the
     B spawn's position. Name it `Cat_B`. Set its layer recursively to RealityB. Put its
     SpriteRenderer(s) on `B_Gameplay` with a cold tint override; set Cat_A's to `A_Gameplay`
     with a warm tint (e.g. #E8A15A). Wire Cat_B's CatRespawn / spawn refs to B objects.
     Do **not** add GravityDebugControl to Cat_B.
  6. Cameras (at scene root, per §3.1).
     - Rename the existing camera to `Camera_A`: culling mask = RealityA | UI, solid-color
       background warm dark (#2A1E14), CatCameraFollow target = Cat_A.
     - Create `Camera_B` by instantiating Camera_A's GameObject (keeps URP camera data and
       CatCameraFollow): remove its AudioListener, culling mask = RealityB | UI, background cold
       dark (#0B1420), follow target = Cat_B, position = Cat_B's position at Camera_A's z,
       Camera component **disabled**.
  7. Lights: create `RealityRoot_X/Lights/GlobalLight_X` (Light2D, Global) targeting only that
     reality's four sorting layers (via the verified serialized property). Warm for A
     (#FFD9A6, intensity 1), cold for B (#A6CCFF, intensity 1). Disable any other Global
     Light2D in the scene and log its path.
  8. Observers: ensure `Observers/Observer_B` with ObserverContext (id = B, cat = Cat_B). Wire
     `reality` and `observerCamera` on both contexts, and `ObserverSet.observerB`. Add
     RealityViewDebugToggle to `Observers` and wire it.
  9. Log a summary; a second run logs "no changes".
- Assets/_Game/Editor/Setup/RealityIsolationValidator.cs — menu
  `PARALLAX/Validate/Reality Isolation`. Read-only; changes nothing. Reports, per root:
  - objects not on the root's layer;
  - SpriteRenderers on another reality's sorting layers;
  - any serialized object reference under one root pointing into the other root
    (iterate SerializedObject properties);
  - Light2Ds under a root targeting another reality's sorting layers;
  - cameras whose culling mask includes both reality layers.
  Ends with "Reality isolation: OK" or a count of problems. RealitySetup calls it as its last step.
  (It's reusable for every future level scene.)

Create — Parallax.Tests.EditMode
- RealitySpaceTests.cs:
  - Origin(A) is zero and Origin(B) is OffsetB;
  - MapTo A→B then B→A round-trips;
  - MapTo same→same is identity;
  - a point's local coordinates under A equal its mapped point's local coordinates under B;
  - layer and sorting-layer names are exact.

Modify (existing — confirm paths)
- CatMotor2D.cs: the ground filter mask comes from the cat's parent reality
  (`GetComponentInParent<RealityRoot>()` in Awake → PhysicsMask), not from CatMotorConfig.
  If there is no parent RealityRoot, log an error with context (no silent fallback). Remove
  `GroundMask` from CatMotorConfig. **No other change to motor math.**
- ObserverContext.cs:
  - Add serialized `reality` (RealityRoot) and `observerCamera` (Camera), with accessors.
  - Awake: log errors if `reality.Id != id` or if the cat is not a descendant of `reality`.
- CatRespawn / FallResetVolume / SpawnPoint / CameraMath: **only if** the pre-read found
  world-space thresholds, cross-reality lookups, or unmasked queries. Make them reality-relative
  or own-reality-masked, and list each change. If none were found, don't touch these files.
- ObserverSetup.cs: find the cat as `Cat_A` (under RealityRoot_A) first, falling back to
  `Cat_Player`, so re-running it after RealitySetup still works and reports "no changes".
- Parallax.Editor.asmdef: add the URP runtime reference if Light2D requires it.

Docs
- 02_ARCHITECTURE.md
  - §3.2: replace "stays world-aligned for now … open question" with "world-aligned, never
    rotates (D-020, confirmed D-021)"; add that hidden cameras are disabled at the component
    level so follow keeps tracking.
  - §6: the ground mask comes from the cat's parent RealityRoot.
  - §3.1: mention `PARALLAX/Validate/Reality Isolation`.
- 07_DECISIONS.md, open-questions list only (no entries edited):
  - Q-2 line → "Resolved by D-020, confirmed on device by D-021."
  - Q-7 line → strike through and add "Closed by D-021."

---

YOU — UNITY EDITOR
1. Recompile; zero Console errors.
2. Open `Sandbox_Realities` (not Sandbox_Player). Run `PARALLAX/Setup/Realities (Sandbox)`.
   Read the log and the validator result ("OK").
3. Run it again → "no changes". Run `PARALLAX/Setup/Observers (Sandbox)` → "no changes".
4. Save the scene.
5. In the build profile, replace Sandbox_Player with Sandbox_Realities in the same slot
   Sandbox_Player used for device builds.
6. Run the acceptance tests.

DO NOT
- Swap drivers between A and B, or add SWITCH (PAX-017). The view toggle only swaps cameras.
- Add the debug panel or picture-in-picture (PAX-018).
- Add anchors, cross-reality interaction, or correspondence-driven gameplay (Phase 4).
- Make GravityDebugControl target B or the viewed Observer.
- Change motor math, input, tuning values, or camera follow parameters.
- Hard-code the offset anywhere except RealitySpace.OffsetB.
- Modify Sandbox_Player, hand-edit .unity/.prefab/.asset/.meta/ProjectSettings, or create art assets.

REQUIREMENTS
- `RealitySpace.OffsetB` is the only definition of the offset; grep confirms it.
- Both cats simulate every tick (A LocalHuman, B Inactive, per the existing bootstrap).
- Each cat's ground detection and every physics query use its own reality's mask.
- Nothing under RealityRoot_B references anything under RealityRoot_A, or vice versa
  (the validator passes).
- Each global light affects only its own reality's sorting layers.
- Exactly one camera renders at a time. Hidden cameras keep following their cat.
- Exactly one AudioListener in the scene.
- Setup is idempotent; the validator is read-only.

ACCEPTANCE TEST

Editor
1. Play. Cat A behaves exactly as in Gate 2: move, jump, all four gravity directions (Q/E/R), respawn.
2. The Scene view shows both realities 1000 units apart. Cat B stands at Spawn_B.
3. Walk Cat A right from Spawn_A past local x = spawn + 3. **No collision** (the pillar exists only in B).
4. Press V → view B. Geometry and cat read cold, background dark blue, Pillar_BOnly visible.
   Label reads "View: B — driving: LocalHuman". Keyboard still moves Cat A (unseen).
   Press V → back to A: warm, and the camera is already on Cat A (no swoop from a stale position).
5. Scene view, Play: drag Cat_B off the edge of its arena → Cat_B respawns **in Reality B** at
   Spawn_B, not in A.
6. Scene view, Play: drag Cat_B right into Pillar_BOnly → it's blocked.
7. Scene view, Play: drag Cat_B down to y ≈ 5 (inside Reality A's space) → no collision with A's
   geometry (the matrix acts as a second line of defense; the offset normally prevents this).
8. `PARALLAX/Validate/Reality Isolation` → OK.
9. EditMode tests all pass (existing + RealitySpaceTests).
10. Console: zero errors. Warnings only the ones expected from setup logs.

Device (Pixel 8a)
11. Build with Sandbox_Realities. Cat A plays as in Gate 2 (stick, jump, debug gravity buttons).
12. View toggle button → B is visibly cold, A visibly warm. Toggle 10× quickly: no black frames
    longer than one frame, no errors in `adb logcat`.
13. FPS stays at the device target with both cats simulating (note the number from the overlay
    or profiler if available; otherwise note "smooth / not smooth").
14. Still owed from PAX-014: half-push vs full-push Move numbers from the stick overlay, if not
    already reported.

DELIVERABLE (from Claude Code)
Standard CLAUDE.md output. Also include:
- the pre-read report (queries, thresholds, reference wiring, Light2D property name);
- every cross-root reference RealitySetup rewired;
- grep output proving the single offset definition.

DEFINITION OF DONE
Universal DoD, plus:
- Docs updated as listed.
- You can explain in two sentences why each reality needs its own sorting layers even though
  cameras already cull by layer, and why the B copies' references had to be rewired.
- Review file sent to the Architect before commit:
  `{ git status; git --no-pager diff; cat <new files>; } > ~/Desktop/pax015_review.txt 2>&1`
- `git status` shows the new .meta files, the modified Sandbox_Realities.unity,
  Cat_Player.prefab (if touched), and CatMotorConfig asset.
- Commit: `PAX-015: two realities, layers, second cat, per-Observer cameras and lights`
