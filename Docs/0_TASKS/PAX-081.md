# PAX-081 · Harness fidelity: the L004 Block_1 kill

**Status:** Approved for implementation.
**Phase 1 size: Lite, read-only.** The CLAUDE.md "Ticket phases and agent budgets" rules apply.
- Phase 1 is the diagnosis in §3: read code, scenes and git history only. It writes no code and runs no new sweeps.
- Report which outcome in §4 applies, then **stop**, except where §4 says to go straight on.

**Phase:** Solo v1, Phase B. This is inserted before KIT-4 (PAX-076), because KIT-4's precision windows are measured by this harness.
**Depends on:** PAX-080 committed (code and docs). The EditMode baseline is 618.

**Decisions:**
- D-057: the 6-tick lead.
- D-066 / D-070: layouts and generated level scenes.
- D-076 (2): the box-model early-flip kill.
- D-079 (4): harness fidelity.
- D-080: L004 Block_1 is recorded there as disputed.
- New **D-081**, written as built.

**Implementer reads first:**
- `Docs/07_DECISIONS.md` (the entries above), `CLAUDE.md`.
- `Docs/0_TASKS/PAX-075.md` §11: amendment 1, rulings R18 and R19.
- `L004Layout.cs`, `L004Routes.cs`, `Editor/Routes/*`.
- `ShippedLevelTimingTests` (the L004 tests), `RoomStepper`.
- Runtime: `ObserverSet`, `RoomManager`, `RoomTrap`, the moving/falling block trap class that `Block_1` uses, and `CatMotor2D`.

**Menus are the developer's job.** Don't run setup menus or modify scenes or assets through MCP.

---

## 0. The developer's report (fill in before Phase 1)

Two separate Editor plays (PAX-078 §9 and PAX-080 §0) were each killed by `Block_1` on an early hop in L004. The harness finds no route that `Block_1` kills (D-079 (4)). Fill in:

- **Input:** keyboard, or the on-screen stick with the mouse? *[fill in]*
- **The hop:**
  - where you took off: stopped before the spikes, or running;
  - whether you held right during the jump;
  - roughly how long you waited first.

  *[fill in]*
- **How often:** how many kills out of how many tries. *[fill in]*

## 1. Why

- The harness is the only thing that measures windows and leads. PAX-078 already had to throw away a box model that disagreed with the game.
- Now the harness disagrees with the game twice, on the same trap.
- KIT-4 (precision sections) will rest entirely on harness-measured windows. So this has to be settled first: either the harness is wrong, or the routes don't cover what a human does.

## 2. What to build

Nothing, until Phase 1 names the outcome. §4 then fixes what gets built.

## 3. Phase 1 (Lite: read-only, cheapest check first)

1. **Scene staleness.**
   - Compare `git log -1` for `Assets/_Game/Editor/Levels/L004Layout.cs` with `git log -1` for `Level_004.unity`.
   - Read `Level_004.unity` through read-only MCP. Do `Block_1` and `Block_2` (position, size, delayTicks, trigger boxes) and `Flip_A` match what `SoloRoomBuilder` builds from `L004Layout`?
   - PAX-078 moved `Flip_A` and retimed the blocks. If the scene predates that, Play runs the old room.
2. **Execution order.**
   - In Play, per fixed step, what is the order of: `ObserverSet.FixedUpdate`; the motor; `RoomManager` and the trap step (`OnLiveRoomStep`); any trap movement in its own `FixedUpdate` or `Update`, or through `MovePosition` or interpolation; and the physics simulation?
   - Compare that order with the harness (`RouteReplay`: `ObserverSet.FixedUpdate`, then `Physics2D.Simulate`).
   - Name every difference, with its file:line.
3. **Input.** Does anything a human can do in the Editor fall outside the route format? For example:
   - a stick magnitude below 1 (a mouse on the on-screen stick);
   - holding the jump button, if the motor reads it;
   - key repeat;
   - the buffer or coyote paths with a held direction.

   Use §0's answers. Name the input family the PAX-075 R18 sweeps didn't cover.
4. **Files.** List the allowed files for the outcome you name (§4).

## 4. Outcomes, decided now

- **(a) The scene is stale.**
  - Go straight to Phase 2.
  - Add a guard test: for every `LevelLayouts` entry, the saved `Level_NNN` scene's traps match the layout (position, size, delayTicks, trigger boxes, repeat settings). See it red on the current stale scene before the rebuild.
  - The developer runs `Rebuild All Levels` and replays L004.
  - D-081 records the guard. The harness stands, and D-080's Block_1 line becomes "kills no route".
- **(b) The execution order differs.** **Stop** with the exact difference and a proposed harness change.
  - The game is the truth, so the harness changes to match it.
  - Every pinned number (`ShippedRouteResultsTests`, TrapLab3, TrapLab4) can move. That needs a ruling.
- **(c) An input family isn't covered** (for example a partial stick).
  - Go straight to Phase 2.
  - Extend the route format minimally (for example `Hold(Right, magnitude)`), then author the human hop as an L004 `Block_1` betrayal.
  - The lead must be ≥ 6. **If the lead is under 6, stop.** That's a D-057 violation in a shipped level, and it needs a layout ruling.
  - `ShippedRouteResultsTests`' L004 case may gain this betrayal's lead. That change is approved by name.
- **(d) None of the above explains it.** **Stop.** Size a dev-only Play trace:
  - it records the commands the motor pulls, and per-tick state, in Play (DebugTools, excluded from release builds);
  - the harness replays those commands and reports the first tick where the two diverge.

  Don't build it until I rule.

## 5. Rules

- The game is the truth, and the harness follows it.
- No runtime (Gameplay or Core) change in any outcome without a ruling.
- No change to L001–L004 geometry.
- The thresholds (12, 6, 30, 0.12 u) don't change.
- Deterministic tests only. Tests that open scenes restore the Test Runner's scene (CLAUDE.md).
- New validator rules and tests use separately named methods.

## 6. Tests (Phase 2, depending on the outcome)

Mark every test "seen red" (quote the failure) or "unproven".
- **(a):** the scene-parity guard is seen red on the stale scene, and green after the developer's rebuild.
- **(c):**
  - a route-format test for the new input, seen red against full magnitude;
  - the L004 `Block_1` betrayal dies at `Block_1` with a lead of ≥ 6;
  - L001–L003 and the Trap Lab results are unchanged.
- **Always:** everything else stays green. Total = 618 + new tests. Added time is ≤ 10 s.

## 7. Allowed files

- **Phase 1:** none; read-only.
- **(a):** a new test file under `Assets/_Game/Tests/EditMode`. `Level_00N.unity` is changed only by the developer's `Rebuild All Levels` (fileID churn accepted, D-070).
- **(c):**
  - `Editor/Routes/*` (the input extension only);
  - `L004Routes.cs`;
  - new tests;
  - `ShippedRouteResultsTests.cs` (the L004 case only).
- **Docs:**
  - `Docs/07_DECISIONS.md`: D-081;
  - `Docs/08_V1_ROADMAP.md`: the PAX-081 row;
  - the L004 comment lines.

## 8. D-081 (write as built)

L004 Block_1 fidelity (PAX-081).
- **Cause:** [(a) / (c)] [exact finding].
- **What changed:** [the guard or the route-format extension].
- **Block_1:** [kills no route / a betrayal with lead N].
- **D-079 (4) and D-080:** [corrected line].

## 9. YOU: Unity Editor

1. **(a):**
   - save the open scene, then run `PARALLAX/Setup/Levels/Rebuild All Levels`;
   - `git status`: `Level_001`–`Level_004.unity` are modified, with fileID churn only;
   - play L004 and hop early over the spikes three times, the way you did before. Report whether `Block_1` still kills you.
2. **(c):** play L004 with the input from §0 again. Report whether you die at `Block_1`, as the new betrayal route predicts.
3. Test Runner, run All (EditMode): expect 618 plus the new tests, all green.

## 10. Required output

As `CLAUDE.md`, plus:
- the Phase 1 findings (§3.1–3.4) with file:line references;
- the outcome letter;
- the test names, each marked "seen red" or "unproven";
- the D-081 text;
- the added test time;
- `pax081_review.txt` and `pax081_review_short.txt` (≤ 300 lines);
- `git add` commands, with docs in a separate commit.

Commit: `PAX-081: L004 Block_1 harness fidelity (D-081)`.