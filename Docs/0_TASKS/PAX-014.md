TASK: PAX-014
TITLE: Observer model, drivers, tick ownership, device-level input rig
PHASE / GATE: Phase 3 / toward Gate 3

OBJECTIVE
Introduce the two-Observer model with LocalHuman and Inactive drivers. Move per-tick
stepping out of CatMotor2D into a single ObserverSet, and move movement input off the
cat onto a device-level rig that the active LocalHumanDriver binds to its cat. After this
ticket, the cat prefab contains no input and a second cat can be created from it cleanly
(PAX-015).

CONTEXT
- 02_ARCHITECTURE.md §4 (Observer model), §4.1 (CatCommand), §5.1 (Movement input),
  §6 (Motor), §10 (Time).
- Current state (Gate 2 handoff 2026-09-16): CatMotor2D.FixedUpdate pulls from a serialized
  `commandSource` → CatInputRouter (merges KeyboardCatInput + TouchStickCatInput).
  TouchStickCatInput derives catRight from a GravityReceiver wired by TouchInputSetup.
  Input components may exist only as scene overrides on Cat_Player in Sandbox_Player.
- `Parallax.Gameplay.Input` namespace stays as is (rename is out of scope).

---

STEP 0 — YOU, BEFORE CLAUDE CODE STARTS (Phase 2 close-out)

1. Open Sandbox_Player. Clear the Console and trigger a recompile (edit/save any script,
   or Assets → Refresh). Confirm **zero** errors. If any remain, stop and report them
   before PAX-014.
2. **Do not** apply Cat_Player's prefab overrides. The setup script in this ticket
   handles the input components. Leave GravityDebugControl as it is.
3. Commit and push the Gate 2 state (PAX-013a/b, D-021) so PAX-014 has a clean rollback
   point.

---

CLAUDE — CODE

Before editing: grep the project for `commandSource`, `ICatCommandSource`, `FixedUpdate`
(under `_Game/`), `TouchInputSetup`, and `GravityReceiver` consumers. Confirm the existing
file paths below. If any existing file lives somewhere materially different, or something
other than CatMotor2D consumes `commandSource`, **stop and report**. List every remaining
`FixedUpdate` under `_Game/` in the deliverable.

Create — Parallax.Core
- Assets/_Game/Core/Observers/ObserverId.cs — `enum ObserverId : byte { A = 0, B = 1 }`
  plus `ObserverIdExtensions.Other()`.
- Assets/_Game/Core/Observers/InputSourceKind.cs — `Inactive, LocalHuman, RemoteHuman, EchoReplay`.

Create — Parallax.Gameplay
- Assets/_Game/Gameplay/Observers/IObserverDriver.cs — the architecture §4 interface
  (`Kind`, `Activate(ObserverContext)`, `Deactivate()`, `FixedTick(int tick)`).
- Assets/_Game/Gameplay/Observers/ObserverContext.cs — MonoBehaviour. Serialized: `id`,
  `cat` (CatMotor2D). Exposes `Id`, `Cat`, `Gravity` (the cat's GravityReceiver, cached
  in Awake), `Driver`, `SetDriver(IObserverDriver)` (Deactivate old → assign → Activate new),
  and `Step(int tick)`, which calls `Driver?.FixedTick(tick)`. **No** RealityRoot or Camera
  fields yet (PAX-015/016 add them).
- Assets/_Game/Gameplay/Observers/ObserverSet.cs — MonoBehaviour. Serialized `observerA`
  (required) and `observerB` (optional until PAX-015; log a single warning if missing).
  `Get(ObserverId)`. Owns `public int Tick`, which starts at 0 and increments once per
  `FixedUpdate`. Each FixedUpdate: `Tick++`, then `observerA.Step(Tick)`, then
  `observerB?.Step(Tick)`. **This is the only per-tick entry point for cats.** No singleton,
  no static access.
- Assets/_Game/Gameplay/Observers/LocalHumanDriver.cs — plain C# class. Constructor takes
  `CatInputRouter`.
  - Activate: bind the router to `observer.Gravity`, then reset the router's transient state.
  - FixedTick: `Read()` the router **exactly once**, then `observer.Cat.Step(cmd, Time.fixedDeltaTime)`.
  - Deactivate: reset the router's transient state.
- Assets/_Game/Gameplay/Observers/InactiveDriver.cs — plain C# class. FixedTick calls
  `observer.Cat.Step(CatCommand.None, Time.fixedDeltaTime)`. It never touches input.

Create — Parallax.App (create the asmdef per §2 if missing; references Core + Gameplay only)
- Assets/_Game/App/ObserverBootstrap.cs — MonoBehaviour composition root for sandbox scenes.
  Serialized refs: `ObserverSet`, `CatInputRouter`. In `Start`: A → `new LocalHumanDriver(router)`;
  B (if present) → `new InactiveDriver()`. Missing refs are logged with context. No throws.

Create — Parallax.DebugTools (dev-only, same defineConstraints as existing debug tools)
- Assets/_Game/DebugTools/ObserverDriverDebugToggle.cs — toggles Observer A between
  LocalHuman and Inactive. Triggered by the T key and by an OnGUI button in the upper-left
  (clear of the stick at bottom-left and the gravity buttons at upper-right). Shows A's
  current `Driver.Kind` and `ObserverSet.Tick`.

Create — Parallax.Editor
- Assets/_Game/Editor/Setup/ObserverSetup.cs — menu `PARALLAX/Setup/Observers (Sandbox)`.
  Must be idempotent. When run, it:
  1. Ensures a `DeviceInput` scene object with CatInputRouter + KeyboardCatInput +
     TouchStickCatInput, with the router's sources wired.
  2. Removes those three components from the Cat_Player scene instance **and** from the
     Cat_Player prefab asset (via PrefabUtility) if present. It does not touch
     GravityDebugControl, CatRespawn, or anything else.
  3. Ensures `Observers` (ObserverSet + ObserverBootstrap) and `Observer_A`
     (ObserverContext id=A, cat=Cat_Player), wired.
  4. Ensures ObserverDriverDebugToggle exists (on `Observers`).
  5. Logs a summary of what changed; a second run logs "no changes".
  Add a reference to Parallax.App in the Editor asmdef if needed.

Create — Parallax.Tests.EditMode
- ObserverIdTests.cs — `Other()` for A and B, and `Other().Other() == id`.

Modify (existing — confirm paths)
- Assets/_Game/Gameplay/Player/CatMotor2D.cs
  - Remove the `commandSource` field and remove `FixedUpdate`.
  - Add `public void Step(in CatCommand command, float dt)` containing the former FixedUpdate
    body (including `gravity.FixedTick(dt)`), with **no change to movement/jump/ground math**.
  - Tick-scoped state (coyote/buffer timers, etc.) must behave identically when Step is
    called once per fixed tick.
- ICatCommandSource.cs — add `void ResetTransientState();`, which clears latched edges,
  current move, stick vector, and finger state (anything that must not survive a handover).
- KeyboardCatInput.cs — implement ResetTransientState.
- TouchStickCatInput.cs
  - Implement ResetTransientState and reuse it from OnDisable (single source of truth).
  - Replace the RequireComponent/sibling GravityReceiver lookup with
    `public void SetGravityFrame(GravityReceiver g)`; read `g.Direction` live each Update.
  - With no frame bound, Move = 0 and a single warning is logged; no exceptions.
- CatInputRouter.cs — implement ResetTransientState (forward to all sources) and add
  `SetGravityFrame(GravityReceiver)` (forward to the touch source).
- TouchInputSetup.cs (Editor) — stop wiring input onto the cat. Either delegate to
  ObserverSetup's DeviceInput step or reduce it to that. It must not re-add input to Cat_Player.

Docs (Claude Code may edit .md)
- Docs/07_DECISIONS.md
  - First verify the latest D-number and Q-number. The handoff says through D-021 and
    Q-8, with a Q-7 numbering note. If they differ, renumber the entries below and report.
  - Append D-022 and Q-9 **verbatim** as given at the end of this ticket.
- Docs/02_ARCHITECTURE.md
  - §4: note ObserverSet owns Tick and steps A then B; drivers call `CatMotor2D.Step`;
    "motor enabled/disabled" means "driver calls / doesn't call Step".
  - §5.1: replace TouchCatInput with the device-level rig (CatInputRouter + KeyboardCatInput +
    TouchStickCatInput), bound by LocalHumanDriver.

---

YOU — UNITY EDITOR
1. After Claude Code finishes: let Unity recompile; zero Console errors.
2. Open Sandbox_Player. Run `PARALLAX/Setup/Observers (Sandbox)`. Read the log.
3. Run it again → "no changes".
4. Inspect: Cat_Player (scene and prefab asset) has no input components. DeviceInput,
   Observers, and Observer_A exist and are wired. Save the scene.
5. Run the acceptance tests below.

---

DO NOT
- Add RealityRoot, a second cat, per-Observer cameras, layers, or sorting layers (PAX-015/016).
- Add a SWITCH controller or button (PAX-017). The debug toggle is not a switch.
- Create EchoReplayDriver or RemoteProxyDriver classes, even as stubs (pending Q-8).
- Change motor physics math, VirtualStick, StickProjection, or any tuning values.
- Modify GravityDebugControl or apply it to the prefab.
- Rename the `Parallax.Gameplay.Input` namespace.
- Add singletons, static accessors, or `[DefaultExecutionOrder]` workarounds.
- Hand-edit .unity / .prefab / .meta / ProjectSettings.

REQUIREMENTS
- Exactly one per-tick entry point for cats: `ObserverSet.FixedUpdate`.
- CatMotor2D has no FixedUpdate and no reference to any input type.
- LocalHumanDriver calls `Read()` exactly once per FixedTick.
- InactiveDriver never calls `Read()`.
- ResetTransientState runs on LocalHumanDriver Activate and Deactivate. Jump presses made
  while Inactive never fire after reactivation.
- The Cat_Player prefab contains no input components.
- Touch projection follows the bound cat's live gravity direction, so D-021 cat-relative
  behavior is unchanged.
- Setup menu is idempotent. All refs are validated with contextual logs, no silent nulls.
- Debug toggle is excluded from release builds.

ACCEPTANCE TEST

Editor
1. Play. Keyboard move and jump feel identical to before PAX-014.
2. Q/E rotate gravity; walk onto a wall. "Right" still walks forward along the surface (D-021).
3. Hold right, press T → A shows Inactive. The cat decelerates to a stop and stays grounded
   or falls under its own gravity.
4. While Inactive, press jump 5×. Press T → LocalHuman. **No jump fires.**
5. While Inactive, rotate gravity (E). Return to LocalHuman and walk. Movement is correct
   for the new gravity.
6. Walk into a fall volume in both driver states. Respawn works.
7. Debug toggle shows Tick increasing about 50/s at the default fixed timestep.
8. All EditMode tests pass (existing + ObserverIdTests).
9. Zero Console errors or warnings, except the single "observerB missing" warning.

Device (short, required — the input rig moved, and EditMode can't catch touch lifecycle bugs)
10. Build to the Pixel 8a. Stick movement and the jump button work; overlay is visible.
11. Rotate gravity with the debug buttons, then walk on a wall. Still cat-relative.
12. Tap the Inactive toggle, spam the jump button, toggle back. No phantom jump.
13. Background the app mid-drag, resume. No phantom movement.
14. Piggyback (Gate 2 open item): watch the overlay's Move readout at half push vs full push.
    Write both numbers in the deliverable notes.

DELIVERABLE (from Claude Code)
Standard CLAUDE.md output. Also include: the list of remaining `FixedUpdate` methods under
`_Game/`; the final D-/Q- numbers used; and confirmation that the CatMotor2D Step body is a
move, not a rewrite (show the diff hunk).

DEFINITION OF DONE
Universal DoD, plus:
- D-022 and Q-9 are recorded; 02_ARCHITECTURE §4/§5.1 are updated.
- Project-knowledge copy of 07_DECISIONS.md is re-uploaded (it currently stops at D-019/Q-6).
- You can explain in two sentences why the motor no longer ticks itself, and why input
  isn't on the cat.
- Review file sent to the Architect before commit:
  `{ git status; git --no-pager diff; cat <new files>; } > ~/Desktop/pax014_review.txt 2>&1`
- Commit: `PAX-014: Observer model, drivers, ObserverSet tick, device-level input rig`

---

TEXT TO APPEND TO Docs/07_DECISIONS.md (verbatim, adjust numbers only if verified different)

### D-022 · 2026-09-16 · Accepted
**Decision:** (1) `ObserverSet.FixedUpdate` is the only per-tick entry point for cats. It owns `Tick` and steps Observer A, then B, through their drivers. `CatMotor2D` has no `FixedUpdate`; it exposes `Step(in CatCommand, float dt)`, called only by drivers. (2) Movement input is a device-level rig (`CatInputRouter` + sources), not part of the cat prefab. `LocalHumanDriver` binds the rig to its Observer's `GravityReceiver` on Activate and resets transient input state on Activate and Deactivate. (3) `EchoReplay` and `RemoteHuman` exist as enum members only; driver classes are written in their own tickets.
**Why:** Explicit step order replaces execution-order coupling and gives Echo a single tick source. A per-cat input rig would make two cats read the same touches, and inactive cats would accumulate stale jump latches that fire on switch-back. Stubs are deferred pending Q-8.
**Supersedes:** 02_ARCHITECTURE §5.1 (per-cat `TouchCatInput`); the handoff note to apply input components to the Cat_Player prefab.

**Q-9** Is v1 co-op-only? If levels require two humans (hostile anchors: one player's action sets traps in the other's reality), Echo (D-007, PAX-023/024) and solo as a first-class mode (Vision §4, D-016 solo cohort, Gate 3 check 6) may be cut. If solo stays, every hostile anchor must remain solvable against a ≤10 s Echo, which rules out traps that depend on the partner not knowing. Unaffected either way: PAX-014–022. SWITCH survives at least as a dev tool, because the one-phone proof (D-011) needs one human to drive both cats. **Decide before PAX-023**, after PAX-022 runs a cross-reality anchor on device.
