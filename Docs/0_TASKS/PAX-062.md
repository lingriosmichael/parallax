# PAX-062 · Levels 31–40 (hard tier, batch 3)

**Status:** Draft. Becomes approvable when the room table (§3) is filled and approved.
**Phase:** Solo v1, Phase D (content)
**Depends on:** PAX-061, PAX-058
**Decisions:** as PAX-059, plus D-062 (3), D-065 (provisional), D-066
**Implementer reads first:** PAX-059, `TierRulesConfig` (Hard), `VariantGroup`, the latest
margin report.

---

## 1. Why

Batch 3 of the hard tier: combine memory, execution and luck in the same level. Difficulty from memory (longer betrayal chains),
execution (Hard tier rules, provisional D-065) and luck (D-062 guardrails, PAX-058).

## 2. What to build

`L031`–`L040` as `LevelLayout` data, tier Hard, appended in order.

## 3. Room table (developer fills and approves before implementation)

| # | One sentence: setup → obvious route → betrayal(s) → learned solution | Memory / Execution / Luck | Traps used | Betrayals |
|---|---|---|---|---|
| 31 | | | | 6–9 |
| … | | | | |
| 40 | | | | |

Mark each level's difficulty source(s). A level that uses Execution notes which jump or window
sits near the Hard limit, so PAX-069 can find it.

## 4. Rules

As PAX-059, but Hard tier rules. Every variant of every `VariantGroup` passes on its own.
Keep near-limit elements few and named: the more levels lean on the provisional numbers, the
bigger the Phase H rework if the phone disagrees (D-064 risk).

## 5. Tests (EditMode)

All-levels fixture passes for L031–L040 against Hard rules. Paste the margin report and highlight
elements within 2 ticks / 0.03 reach of the Hard limit.

## 6. Allowed files

As PAX-059.

## 7. Out of scope

As PAX-059. A needed kit gap becomes its own ticket first.

## 8. YOU — UNITY EDITOR

Run All, report total. Play L031–L040 from the menu; log designer deaths in
`Docs/Playtests/designer_L031-040.md`; replay any luck death from its logged seed to confirm it's
reproducible.

## 9. Required output

As `CLAUDE.md`, review file `pax062_review.txt`, room table, margin report.
Commit: `PAX-062: levels 31–40 (hard, batch 3)`.
