# PAX-046 · Rooms v3: kit v2 in Level_Solo01, platforms, disguised betrayals

**Status:** Approved for implementation (Phase 1 gate first, see §9)
**Phase:** Solo v1 (D-040, D-041, D-050, D-053, D-054, D-055, D-056, D-057)
**Depends on:** PAX-045 committed; `Docs: record D-056` and `Docs: record D-057` committed (§3)
**Decisions:** D-040, D-041, D-044, D-050, D-053, D-054, D-055, D-056, D-057
**Implementer reads first:** `Docs/07_DECISIONS.md` (newest wins), `CLAUDE.md`, then as built:
`SoloRoomsLayout` (the four rooms), `SoloRoomsSetup`, `TrapLayoutValidator`, `TrapLabLayout`
(kit v2 examples), `SoloRoomsLayoutTests`, `TrapKitV2Tests`. Where this ticket assumes a name or
signature that differs from the code, **the code wins: adapt and report it.** Where the difference
changes behaviour, stop and report.

---

## 1. Goal

`Level_Solo01` still uses kit v1 only, on mostly flat floors. Rebuild its four rooms so they play
like Level Devil: **the room mutates as you solve it**, the obvious move is the one that kills
you, and the solution is learnable after one death.

- Use kit v2 (chains, moving traps, repeats) across the level.
- Add **real platforming**: elevated platforms, gaps and height changes, not just a flat run.
- **Disguise** every betrayal until it fires.
- Raise difficulty through **stacking and disguise**, not through tighter jumps.

**Layout data only.** No runtime or setup code changes. If a room needs something the kit can't
do, it goes on the kit-gap list (§11) and the room is designed without it.

## 2. What stays true

- Room frame: four rooms, W 32, origins 0 / 45 / 90 / 135, same floor/ceiling frame as PAX-044.
- D-050: room N+1's checkpoint is unreachable before room N's door. Rooms joined only by door
  teleports.
- D-053: no tutorial rooms, stacked and disguised betrayals, visible tools, no soft-locks.
- D-054: `SoloRoomsLayout` is the only source; the scene is rebuilt from it, never tuned by hand.
- PAX-044 layout rules 1–8 and the reachability contract (test 8) stay in force.
- D-055: every trap passes `TrapLayoutValidator`.

## 3. Decisions D-056 and D-057

D-056 is committed; the text in `Docs/07_DECISIONS.md` is authoritative (it includes the
from-rest timing rule and the Solid launch rule). D-057 amends D-056 (5); commit it before
Phase 1 resumes:

```
### D-057 · 2026-09-22 · Accepted
**Decision:** Amends D-056 (5). Death reset stays synchronous (RoomDeath.Kill), so nothing is
visible after a death. Learnability is therefore met before the kill: every betrayal that can
kill shows its visible change (a gap opening, spikes revealed, a block or Solid moving, a door
moving) at least 6 ticks (0.1 s at 60 Hz) before it can kill. The other betrayals keep D-056 (5)
as written: their result stays visible until the room resets. A death hold that shows the fired
room state after a death is a separate runtime ticket (PAX-047); once it lands, this rule is
reviewed.
**Why:** The death frame is never rendered, so "visible after death" was unachievable with the
kit as built. 6 ticks is long enough to see and shorter than a human reaction (≈ 15 ticks), so
a betrayal still kills the first time but the player saw what did it.
```

## 4. Room requirements

Per room:

- A **one-sentence shape**: setup → obvious route → betrayal → learned solution (vision §4).
- **4–6 betrayals**, at least **2 of them kit v2** (chained, moving, or repeating).
- At least **2 required platform jumps** with a height change (up or down), recorded as
  `RequiredJumps`.
- At least one **"false safety" moment**: a spot that looks safe (a platform, a gap already
  crossed, a trap that already fired) that is itself the next betrayal, or triggers it.
- A designer estimate of **deaths for a first-time player: 3–6**.

Across the level:

- At least one **chain of 3+ links**, one **moving Solid** (crush or rising floor), one **moving
  Hazard**, one **Periodic** trap, one **Rearm** trap, and one **gravity-flip** use.
- Difficulty **rises** room to room; Room 4 combines what the first three taught.
- Keep PAX-044 betrayals that work; replace the weakest. Say which and why.

## 5. Design notes

- **Chains are the core tool.** The best Level Devil moments are "I dodged it, and dodging it
  was the trigger". A chain whose source is the player's survive move is worth more than a new
  trap type.
- **Rearm and Periodic are for rooms the player crosses more than once**, or where waiting is the
  obvious move. A rhythm the player can read is fair; a rhythm hidden behind a disguise is not.
- **Moving Solids** are strongest as floors that leave (drop, slide away) or walls that close.
  Mind the launch when a fast Solid stops under the cat (PAX-045: 9.7 u/s up launched the cat).
- **Platforms carry the trolls.** A platform that collapses, rises into spikes, or triggers a
  block is the natural v3 betrayal.
- **No sideways carrying platforms** (kit gap). Vertical Solids carry the cat; horizontal ones push.

## 6. Layout data

- All changes in `SoloRoomsLayout`: elements, settings, openings, hazard roles, `RequiredJumps`,
  `RequiredSteps`.
- Platforms are `Floor` elements at height (or `Wall` where they are side geometry). Every
  platform edge that the reachability contract uses is an element edge, so a moved platform
  breaks the test instead of the record.
- Crush partners: name them in the layout (a `SourceName`-style field or a naming convention the
  test can read; propose which in Phase 1).

## 7. Out of scope

New trap types or kit changes · runtime or setup code · level-complete screen · death counter UI
· art (PAX-A06/A07) · device testing · Reality B · co-op · new rooms beyond four.

## 8. Tests (EditMode)

Extend `SoloRoomsLayoutTests` (reflection, as today) or add a new file under `Tests/EditMode`:

1. **All four rooms pass `TrapLayoutValidator`.**
2. **Kit v2 coverage:** each room has ≥ 2 kit v2 traps; the level has every feature in §4.
3. **Platform jumps:** each room has ≥ 2 `RequiredJumps` with a height change; all pass the
   existing reachability contract.
4. **Periodic windows (D-056 (1)):** for every Periodic hazard on the route, the safe window
   (period − cooldown) ≥ crossing ticks + 12. Crossing is always computed **from rest** (the
   player waits in front of a rhythm): d = hazard width + cat width, a = measured acceleration
   (Phase 1 reports it from code/config; do not use the inferred 12 u/s²), v = 0.10 u/tick;
   in tick units a_t = a / 3600 u/tick²; ticks = √(2d/a_t) if d ≤ v²/2a_t, else
   v/a_t + (d − v²/2a_t)/v. If the crossing is a required jump,
   use the jump's air time instead. At a = 12 u/s², d = 1.5 u is ≈ 30 ticks, not 15.
5. **Moving Solid paths (D-056 (3)):** the swept box of every moving Solid overlaps no fixed
   geometry element (touching allowed), except its named crush partner, which it may only touch.
5b. **Solid launch (D-056 (3)):** for every moving Solid that moves upward in any leg (move or
   return), v = |Δy| / leg ticks × 60 u/s, rise = v² / 60. The box above its upward-stopped pose,
   cat width wide, rise + 0.56 u tall, overlaps no hazard element.
6. **Disguise (D-056 (4)):** every CollapsingFloor and moving Solid uses the floor/wall look;
   every chained or periodic trap has no trigger box; hidden spikes are not rendered before
   reveal. Check from layout data (settings, kinds, secondary sizes); report anything the data
   cannot express.
6b. **Visible before kill (D-057):** every hidden-spike reveal delay (overlap- or chain-triggered)
   is ≥ 6 ticks; every chained killing trap fires its visible change ≥ 6 ticks before it can
   overlap the cat's route. Check from layout data; list in the report any killing betrayal whose
   lead time the data cannot express.
7. **Regression:** all existing `SoloRoomsLayoutTests` rules (1–8, containment, separation,
   reachability) still pass on the new rooms. If a rule no longer fits a v3 room, stop and report;
   do not weaken it.

Test 4 covers Periodic only. Slack for chain delays, collapse delays and moving traps (D-056 (1))
is room-specific and is checked in the Phase 1 table and in acceptance §13.1, not by a test.

**Baseline:** 266 (PAX-045). Report baseline → new.

## 9. Phase 1 gate (stop before writing code)

Post, then wait for Architect confirmation:

1. **As-built list:** for each current room, its elements and betrayals in order, and which you
   keep, change or drop. The Room 4 ceiling hidden spikes were checked by the developer in the
   Editor (answers handed over with this ticket): use them. If any answer is no, design around them
   and add to the gap list.
2. **v3 table, per room:**
   - the one-sentence shape;
   - every element (kind, name, position, size, settings), platforms included;
   - every betrayal in order: trigger, fire tick, what it does, **kill case** and **survive case**
     with numbers (positions, ticks, windows, slack against the 12-tick rule; from rest wherever
     the obvious move is to wait);
   - every required jump: take-off and landing elements, gap, Δh, ratio against 0.75;
   - every chain: links, delays, fire ticks relative to the source;
   - every moving Solid that can carry the cat: peak upward speed, launch rise, and where the
     launch lands;
   - for every betrayal that can kill: its visible change and the lead time (ticks) before it
     can kill, against the 6-tick rule (D-057);
   - the "false safety" moment;
   - designer death estimate.
3. **Crush partner convention** (§6) and anything the layout data can't express.
3b. **Measured acceleration:** the cat's ground acceleration (and deceleration, if separate) from
   code or config, with the file and field. Test 4 and every from-rest window use it.
4. **Allowed-files check** (§10).

Use the measured numbers (60 Hz): run 0.10 u/tick, gravity 30, jump 3.2 u, R ≈ 5.54 u, air time
≈ 55 ticks, cat 1.0 × 0.56 (paw line −0.4), blocks 0.3 u/tick, crush depth 0.15 u.

## 10. Allowed files

- `SoloRoomsLayout.cs` (room data only)
- `SoloRoomsLayoutTests.cs` and new test files under `Tests/EditMode`
- `Level_Solo01.unity`: rebuilt by the developer only

Nothing else. If a room seems to need a code change, stop and ask.

## 11. Kit-gap list (keep it going)

Carry over the list in `HANDOFF.md` §4. Add anything the v3 rooms wanted but couldn't have, one
line each on what it would take.

## 12. YOU — UNITY EDITOR

1. Before handing the ticket over: run EditMode, confirm 266. In `Level_Solo01`, play Room 4,
   flip and walk the ceiling under the hidden spikes: are they invisible before reveal, do they
   point down, do they kill? Hand the three answers over with the ticket.
1b. Commit `Docs: record D-056` (text in §3, as patched).
2. After Phase 2: Cmd+R, then EditMode → Run All. Send the total and any failures **with** the
   review file (upload it to the chat).
3. In `Level_Solo01`: delete `Room_1…4`, run `PARALLAX/Setup/Solo Rooms (PAX-043)`, Cmd+S.
4. Play `Level_Solo01`, run §13.
5. Blind playtest (2–3 people if possible): deaths per room, where they died, whether they could
   say what killed them.

## 13. Acceptance (Editor)

1. **Every kill case kills and every survive case survives**, 3 times each, as in the Phase 1
   table.
2. **Every required jump** is makeable on the first or second try by the designer.
3. **Disguise:** before firing, no betrayal is visible as a trap in the greybox.
4. **Learnability (D-057):** before each death the cause visibly changes at least 6 ticks before
   the kill; after each death the designer can say what killed them.
5. **No soft-locks:** no rearm, periodic or moving state leaves the door unreachable.
6. **Chains, moves and repeats** behave as in the table; no cat pushed through geometry.
7. **Reset:** dying mid-chain, mid-move and mid-cycle restores the room completely.
8. **Difficulty:** designer run records deaths per room; rooms get harder 1 → 4.
9. **Console:** known noise only.

## 14. Required output

As `CLAUDE.md` requires, plus: the Phase 1 list and table, which PAX-044 betrayals were kept or
dropped and why, the updated kit-gap list, the test count, and the review file:

```
{ git status; git --no-pager diff -- . ':(exclude)*.unity'; for f in $(git ls-files --others --exclude-standard | grep -v '\.meta$'); do echo "===== $f"; cat "$f"; done; } > ~/Desktop/pax046_review.txt 2>&1
```

Commit after Architect review and acceptance, staged by path: `PAX-046: Rooms v3 (D-056)`.
