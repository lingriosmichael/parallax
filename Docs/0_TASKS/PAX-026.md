TASK: PAX-026 (absorbs PAX-027; PAX-025 tilt is moved after the next device session)
TITLE: Gravity control v1: IGravityControlInput, touch dial, Control Station, control stream, Echo choreography
PHASE / GATE: Phase 5 / Phase 5 "done when"

OBJECTIVE
Cat A sits at a Control Station in Reality A. Movement locks and an on-screen dial appears.
Turning the dial publishes a control stream that steers Cat B's gravity in Reality B. The
station glows immediately; B's gravity follows after the transport delay. Standing up or
switching away leaves B's gravity where it was. A recorded dial choreography replays onto B
through Echo.

ORDER CHANGE
Plan PAX-025 (tilt) needs a phone to build and verify. Device sessions are paused, so this ticket
does the Editor-testable half first: the input interface, the dial (works with a mouse), the
station, the stream, the receiver and Echo. Tilt becomes a later ticket that plugs into the same
interface without changing anything here.

CONTEXT
- 02_ARCHITECTURE.md §5.2 (IGravityControlInput), §5.3 (Control Station), §6 (GravityReceiver;
  only SetTargetDirection changes gravity; mapping is tunable), §7.3 (control streams: latest wins,
  receiver holds), §8.2–8.4 (Echo records and re-publishes control samples), §9.4.
- Plan §10.5 (solo gravity: discrete state and choreography).
- D-022, D-025 (the cat is the single interception point), D-027 (Echo v1), D-028.
- Current state: PAX-024 committed. Editor acceptance for PAX-021/023/024 passed. Gate 3 device
  checks are pending (device sessions paused).

---

STEP 0 — BEFORE CLAUDE CODE STARTS
`git status` clean and up to date with origin/main. Claude Code: check; stop if not.

---

CLAUDE — CODE

PRE-READ (report with quotes before editing; stop if anything contradicts this ticket)
a. `GravityReceiver`: public API (SetTargetDirection signature, snap parameter, Direction), and
   who calls SetTargetDirection today (grep).
b. `LocalHumanDriver` current FixedTick/Activate/Deactivate as committed.
c. `CatInteractor` and `EchoSession`/`EchoReplayDriver`/`EchoPlayback`/`EchoRecording`
   signatures as committed; every caller of `EchoPlayback.Advance` and the `EchoRecording`
   constructor (including tests).
d. `IRealityTransport.PublishControl` / `ControlReceived` in `LocalTransport` (delivery uses the
   same delay rules as anchors).
e. How `SwitchButton`/`EchoRecordButton` are placed in HUD, and the HUD canvas scaler settings.
f. `ObserverIdExtensions.Other()` location.

Create — Parallax.Core (flat namespace Parallax.Core)
- Core/GravityControl/IGravityControlInput.cs — exactly §5.2:
  `bool IsAvailable { get; }`, `void Calibrate();`, `float ReadNormalized();` (−1..1).
- Core/GravityControl/GravityControlMapping.cs — pure static:
  `Vector2 ToDirection(float value, float maxAngleDeg, float snapDeg)`:
  clamp value to −1..1; `angle = value * maxAngleDeg`; if `snapDeg > 0` round angle to the nearest
  multiple of snapDeg; rotate `Vector2.down` **clockwise** by `angle` (positive value = clockwise,
  matching a clockwise dial turn). Return normalized.
- Core/GravityControl/DialMath.cs — pure static:
  `float ValueFromPointer(Vector2 center, Vector2 pointer, float maxDialDeg)`:
  if pointer is within 8 px of center return NaN (caller keeps the previous value); otherwise
  `-Vector2.SignedAngle(Vector2.up, pointer - center) / maxDialDeg`, clamped to −1..1
  (clockwise from 12 o'clock = positive).
- Core/GravityControl/SeatCommandFilter.cs — pure static:
  `CatCommand Apply(CatCommand command)`: Move = 0, JumpPressed = false, JumpHeld = false;
  InteractPressed/InteractHeld unchanged.
- Core/Echo/EchoControlEvent.cs —
  `readonly struct EchoControlEvent { int TickOffset; ControlChannel Channel; ObserverId Target; float Value; }`
  with a constructor.
- Modify Core/Echo/EchoRecording.cs: add `IReadOnlyList<EchoControlEvent> ControlEvents`
  (stable-sorted by TickOffset, like AnchorEvents). Constructor takes a fourth argument.
- Modify Core/Echo/EchoRecorder.cs: `void AddControlEvent(ControlChannel channel, ObserverId target, float value)`
  — same rules as AddAnchorEvent (offset = FrameCount, ignored when full, throws after Finish).
- Modify Core/Echo/EchoPlayback.cs: `EchoFrame Advance(List<EchoAnchorEvent> dueAnchors, List<EchoControlEvent> dueControls)`
  — due control events use the same `<= Cursor` rule; none while holding. Replace the old
  one-argument overload (update all callers and tests; test assertions keep their meaning).

Create — Parallax.Gameplay
- Gameplay/GravityControl/DialGravityInput.cs — uGUI MonoBehaviour, implements
  `IGravityControlInput`, `ITouchReservedRegion`, `IPointerDownHandler`, `IDragHandler`.
  - Serialized: `RectTransform knob`, `float maxDialDeg = 135f`, `GameObject visualRoot`.
  - `IsAvailable` = true. `Calibrate()` sets the value to 0 and the knob to 12 o'clock.
  - Pointer down/drag: convert to the dial's local/screen center, `DialMath.ValueFromPointer`;
    ignore NaN; store the value; rotate the knob to match (only when the value changes).
  - `ReadNormalized()` returns the stored value.
  - `SetVisible(bool)`: toggles `visualRoot`. `ContainsScreenPoint` returns false while hidden.
  - Clears EventSystem selection after pointer up.
- Gameplay/GravityControl/CatSeat.cs — MonoBehaviour on each cat.
  - `ControlStation Station { get; }`, `bool IsSeated => Station != null`.
  - `void SitAt(ControlStation station)`, `void Release()` (calls `station.OnReleased(this)` once).
- Gameplay/GravityControl/ControlStation.cs — MonoBehaviour under a RealityRoot, on a trigger zone;
  implements `IInteractable`.
  - Serialized: `ObserverSet observers`, `MonoBehaviour inputSource` (must implement
    IGravityControlInput; validated in Awake like `reservedRegions`), `DialGravityInput dial`
    (for show/hide; may be the same object as inputSource), `SpriteRenderer glow`,
    `SpriteRenderer transitPulse`.
  - `Interact(observerId, requester)`: get that Observer's cat and its CatSeat.
    If seated here → `seat.Release()`. Else if the station is free and the cat isn't seated
    elsewhere → `seat.SitAt(this)`, remember the occupant Observer, `input.Calibrate()`,
    `lastPublished = 0`, `hasPublished = false`, show the dial.
  - `OnReleased(seat)`: clear occupant, hide dial, glow off.
  - Subscribes to `ObserverSet.Stepped`. Per tick, while occupied **and** the occupant's driver is
    LocalHuman: `value = input.ReadNormalized()`; glow alpha = 0.3 + 0.7·|value| immediately; if
    `!hasPublished && value == 0` do nothing (sitting down never publishes); else if
    `|value − lastPublished| > 0.001` or `!hasPublished && value != 0` → publish through the
    occupant cat's `CatInteractor.PublishControl(ControlChannel.GravityAngle, occupantId, value)`,
    set lastPublished/hasPublished, and start a 0.25 s transit pulse (cosmetic, `Time.time`).
  - Never calls the transport directly and never touches GravityReceiver.
- Gameplay/GravityControl/GravityControlReceiver.cs — MonoBehaviour on each cat.
  - Serialized: `TransportHost transportHost`, `float maxAngleDeg = 90f`, `float snapDeg = 0f`.
  - Lazy init: `RealityRoot` via GetComponentInParent (its Id is this cat's Observer),
    `GravityReceiver` via GetComponent; subscribe to `transportHost.Transport.ControlReceived`
    (OnEnable/OnDisable, lazy if the transport isn't ready in OnEnable).
  - On a sample with `Channel == GravityAngle` and `Target == root.Id`: store `LastValue`,
    `gravity.SetTargetDirection(GravityControlMapping.ToDirection(value, maxAngleDeg, snapDeg))`
    (non-snap). Ignore everything else. It holds the direction when samples stop (does nothing).
  - Read-only `LastValue`, `HasValue` for the panel.
- Modify Gameplay/Interaction/CatInteractor.cs:
  - `public event Action<ControlSample> ControlPublished;`
  - `public void PublishControl(ControlChannel channel, ObserverId source, float value)`:
    build `new ControlSample(channel, source.Other(), value, transport.Tick)`,
    call `transportHost.Transport.PublishControl`, then raise ControlPublished. Log once and return if
    the host/transport is missing.
- Modify Gameplay/Observers/LocalHumanDriver.cs:
  - Activate: cache `CatSeat` from the cat (GetComponent; null = can't sit).
  - FixedTick: `motorCommand = seat != null && seat.IsSeated ? SeatCommandFilter.Apply(cmd) : cmd`;
    step the motor with `motorCommand`; the interactor still gets the unfiltered `cmd`.
  - Deactivate: `if (seat != null && seat.IsSeated) seat.Release();` before the router reset.
- Modify Gameplay/Echo/EchoSession.cs: while recording, subscribe to the recording cat's
  `CatInteractor.ControlPublished` → `recorder.AddControlEvent(sample.Channel, sample.Target, sample.Value)`;
  unsubscribe wherever Requested is unsubscribed. Pass `transportHost.Transport` to the replay driver.
- Modify Gameplay/Echo/EchoReplayDriver.cs: constructor
  `(EchoPlayback playback, IAnchorRequester requester, IRealityTransport transport)` (null checks);
  per tick, for each due control event publish
  `new ControlSample(e.Channel, e.Target, e.Value, transport.Tick)`. Reuse a second due list.

Modify — Parallax.DebugTools
- DebugPanel.cs: add a row per Observer: `Seat <Id>: seated|-  Recv <Id>: <LastValue:F2>|-`
  (serialized references to both cats' CatSeat and GravityControlReceiver, wired by setup).
  Grow PanelRect if needed; still one field.

Create — Parallax.Editor
- Editor/Setup/ControlStationSetup.cs, menu `PARALLAX/Setup/Control Station (Sandbox)`,
  idempotent, using SetupUtility, one statement per line:
  1. `RealityRoot_A/Interactables/Station_A` at local (spawnX + 6, groundTop + 0.8): trigger
     BoxCollider2D 1.2 × 1.6, layer RealityA; ControlStation; child `Base` sprite 1.0 × 0.8
     (A_Gameplay) sitting on the ground; child `Glow` sprite 1.2 × 0.2 above the base (alpha 0.3);
     child `Pulse` sprite 0.3 × 0.3 (alpha 0). If x + 0.6 exceeds A's Ground right bound, shift left
     and log.
  2. `HUD/GravityDial` anchored right-middle (offset about −220, 0), about 260 × 260: background
     Image (built-in UISprite, dark, alpha 0.55), `Knob` child (a 24 × 110 bar pivoted at the
     bottom-center of the dial's center), DialGravityInput wired, `visualRoot` = a child holding the
     graphics, hidden at start. Not overlapping the touch interact rect (0.56–0.74 x, 0–0.40 y).
  3. ControlStation wired: observers, inputSource = dial, dial, glow, pulse.
  4. CatSeat and GravityControlReceiver on Cat A and Cat B; receivers wired to Systems/LocalTransportHost.
  5. DialGravityInput added to TouchStickCatInput.reservedRegions (append if missing).
  6. DebugPanel wired to both seats and receivers.
  Logs changes and computed positions; second run: "ControlStationSetup: no changes."
  Runs RealityIsolationValidator, AnchorValidator and ControlValidator.
- Editor/Setup/ControlValidator.cs, menu `PARALLAX/Validate/Control`, read-only:
  every ControlStation is under a RealityRoot with a matching layer, has a trigger collider,
  observers, and an inputSource implementing IGravityControlInput; every cat under a RealityRoot
  has a CatSeat and a GravityControlReceiver with a host. Logs "Control: OK" or one line per problem.

Create — Parallax.Tests.EditMode
- GravityControlTests.cs, one behavior per test:
  - Mapping: value 0 → down; value 1, max 90 → (−1, 0) (clockwise); value −1, max 90 → (1, 0);
    value 2 is clamped like 1; snap 90: value 0.6 → (−1, 0), value 0.4 → down; result is normalized.
  - DialMath: pointer straight up → 0; pointer right (clockwise 90°) with max 135 → 90/135;
    pointer left → −90/135; pointer below-right beyond max → clamped to 1; pointer within 8 px → NaN.
  - SeatCommandFilter: Move/Jump cleared, Interact kept.
  - Recorder control events: offset = frame count at the call; ignored when full; throws after Finish.
  - Recording sorts control events stably.
  - Playback: due control events on their offset, `<=` rule for past offsets, none while holding,
    anchors and controls due on the same tick both returned.
  - Integration: LocalTransport (tick source) at latency 100 ms; publish a ControlSample during tick 5 →
    ControlReceived fires at tick 10, not 9 (same delay rule as anchors).
  - Integration: replay a recording with control events (offset 0 → 0.5, offset 2 → −0.5) through a
    new EchoPlayback, publishing due events to LocalTransport each tick → ControlReceived values arrive
    in order 0.5 then −0.5; replaying again with a new playback delivers them again.
- Update EchoTests to the new Advance/EchoRecording signatures (assertions unchanged).

Docs
- 02_ARCHITECTURE.md
  - §5.2: "As built (PAX-026)": dial first; tilt deferred to a later ticket on the same interface.
  - §5.3: "As built": CatSeat + SeatCommandFilter in LocalHumanDriver, sit/stand with interact,
    seat released on driver Deactivate, publish on change only via CatInteractor.PublishControl,
    sitting never publishes, glow immediate + transit pulse.
  - §6: GravityControlReceiver applies received samples through GravityControlMapping (maxAngle,
    optional snap; clockwise positive) and holds.
  - §8: Echo records ControlPublished as (TickOffset, Channel, Target, Value) and re-publishes due
    control events during replay; at the end the receiver holds the last value.
- 07_DECISIONS.md: append
  ### D-029 · <date> · Accepted
  **Decision:** (1) Gravity control input is `IGravityControlInput` (Core); the touch dial ships first, tilt plugs into the same interface later. (2) A cat sits at a `ControlStation` via interact; `LocalHumanDriver` filters its motor command (no move, no jump) while seated and releases the seat on Deactivate. (3) Stations publish control samples only through the seated cat's `CatInteractor.PublishControl`, only when the value changes, never on sitting down; the target is the other Observer. (4) Each cat's `GravityControlReceiver` applies samples for its Observer through `GravityControlMapping` (positive = clockwise, per-receiver max angle and optional snap) and holds the last direction. (5) Echo records `ControlPublished` and re-publishes due control events during replay.
  **Why:** One interception point on the cat serves anchors and streams, so Echo records both the same way. Publishing on change keeps recordings small and means sitting down doesn't reset the other cat's gravity (a discrete setting survives standing up and sitting again). Releasing the seat on Deactivate means switching away never leaves a stale occupant.
  **Consequence:** Tilt (plan PAX-025) must only implement `IGravityControlInput` and be selectable; nothing in station, stream, receiver or Echo changes. Mapping lives on the receiver for now; if stations need different mappings, the sample or station config must carry it (decide with the first puzzle that needs two mappings).

---

YOU — UNITY EDITOR (walked through one step at a time)
1. Recompile; zero errors. EditMode tests all green.
2. Sandbox_Realities: run `PARALLAX/Setup/Control Station (Sandbox)` twice (second: no changes). Save.
3. Editor acceptance tests.

DO NOT
- Implement tilt, sensors, calibration UI or a settings screen.
- Network anything, add Photon, or add a ~20 Hz publish rate (PAX-034).
- Let the station call the transport or GravityReceiver directly.
- Change the motor, GravityReceiver, anchors, sensors or switch code.
- Add a second station, stations in B, or per-station mappings.
- Add singletons, static mutable state, runtime Find, or `?.`/`??` on Unity objects.
- Hand-edit .unity/.prefab/.asset/.meta.
- Commit. The Architect reviews first.

REQUIREMENTS
- Only `GravityReceiver.SetTargetDirection` changes gravity; the receiver is the only new caller.
- Glow responds on the tick the dial changes; the other cat's gravity changes only after delivery.
- Sitting down never publishes; standing up and switching away never publish.
- A seated cat cannot move or jump but can stand up with interact.
- Echo replays a dial choreography onto the other cat with the same timing as recorded.
- Validators pass; setup is idempotent; zero new Console warnings.

ACCEPTANCE TEST (Editor; latency 0 unless stated; PiP on to watch B while controlling A)
1. EditMode: all green.
2. Walk A to the station (right of the vine) and press F: the panel shows `Seat A: seated`, the dial
   appears, and A/D and Space do nothing.
3. Drag the dial clockwise: the glow brightens immediately, `Recv B` shows the value, and B's gravity
   turns clockwise in PiP (B slides or falls toward the new "down").
4. Drag back to 12 o'clock: B's gravity returns toward straight down.
5. Latency 150, then 300: the glow stays immediate; B's gravity follows ~0.15 s / ~0.3 s later.
6. Press F: A stands, the dial hides, A moves again, and B's gravity stays where it was.
7. Sit again: B's gravity does not change until you touch the dial.
8. Discrete state (Type A): sit, set the dial to about 3 o'clock, press Tab without recording. A is
   Inactive (seat released), and B is live with rotated gravity and can walk on the wall.
9. Choreography (Type C): Tab back to A. Sit, press Z, sweep the dial slowly left and right for
   about 5 s, press Tab. The timeline runs, and as B you feel the same sweep. After HOLD, B's gravity
   stays at the final value.
10. Step 9 at latency 300: the same sweep arrives delayed.
11. Isolation: as B, stand at the station's location in B and press F: nothing. As A away from
    the station, F does nothing.
12. Tapping or clicking the dial never makes A jump; when the dial is hidden, clicks there behave
    as before.
13. Vine, plate/gate, Echo and gravity debug (Q/E/R) still work. Zero warnings and errors.

Device (deferred until device sessions resume): dial with touch; station glow at 300 ms.

DELIVERABLE (from Claude Code)
Standard CLAUDE.md output, plus:
- pre-read a–f with quotes;
- a tick walk-through at latency 0: dial changes between ticks N−1 and N → publish tick, delivery
  tick, SetTargetDirection tick, and when GravityReceiver starts turning;
- every position ControlStationSetup computes;
- grep proof, verbatim:
    grep -rn "SetTargetDirection" Assets/_Game --include=*.cs
    grep -rn "PublishControl" Assets/_Game --include=*.cs
    grep -rn "Photon\|Fusion" Assets/_Game
    grep -rn "Find" Assets/_Game/Core Assets/_Game/Gameplay
    grep -rn "??\|?\." Assets/_Game/Gameplay/GravityControl
  (for each ?./?? hit, name the type)
- D-number used; new EditMode test count.
- Review file:
    { git status; git --no-pager diff; cat <every new .cs file>; } > ~/Desktop/pax026_review.txt 2>&1

DEFINITION OF DONE
Universal DoD, plus:
- You can explain in two sentences why the station publishes through the cat instead of the
  transport, and why sitting down must not publish.
- Architect review passed before commit:
  `PAX-026: gravity dial, Control Station, control stream, Echo choreography (D-029; absorbs PAX-027)`
