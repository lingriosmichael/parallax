TASK: PAX-021 (absorbs PAX-022)
TITLE: Presenters and manifestations; cross-reality interaction (vine in A raises elevator in B)
PHASE / GATE: Phase 4 / toward Gate 3

OBJECTIVE
The first action in one reality changes the other. Cat A pulls a vine, the request goes through
the transport, the authority commits the anchor, and an elevator in Reality B rises. After a
switch, Cat B can stand on it and reach a ledge it can't jump to. This must work at 300 ms of
artificial latency, on the Pixel 8a.

CONTEXT
- 02_ARCHITECTURE.md §3.1 (reality roots, Anchors/ folders), §4 (tick ownership), §5.1 (input),
  §7.1 (anchors), §7.2 (presenters and manifestations), §8.3/§8.5 (why Echo needs a
  per-Observer interception point and overlap queries), §9.1 (LocalTransport), §9.4 (latency rules).
- Plan §10.4 Type A (state puzzle: A acts, switch, B sees the result; no Echo needed).
- D-022 (ObserverSet owns the tick; drivers call the motor), D-023 (switch ownership, reserved
  touch regions), D-024 (per-origin sequences, never-synchronous transport; a request made during
  tick N is delivered no earlier than tick N+1).
- Current state: PAX-019 committed. Sandbox_Realities has Systems/LocalTransportHost wired to
  ObserverSet and DebugPanel. 75 EditMode tests pass.

This ticket records D-025 (below). It deliberately narrows two lines in the docs:
- 02_ARCHITECTURE §7.2 says interactables issue requests "through the transport".
- CLAUDE.md says "Puzzles talk only to IRealityTransport".
Under D-025, interactables issue requests through an `IAnchorRequester` handed to them by the
interacting cat. The requester sits in front of the transport. This is intended, not a conflict;
update both docs as listed under Docs.

---

STEP 0 — BEFORE CLAUDE CODE STARTS
1. `git status` is clean.
2. Push: `git push` (5+ local commits are not backed up yet). `git status` must say
   "up to date with 'origin/main'".
Claude Code: check both. If either is not true, stop and report.

---

CLAUDE — CODE

PRE-READ (report before editing; stop if anything contradicts this ticket)
a. `CatCommand`: are `InteractPressed` / `InteractHeld` populated anywhere today? Quote how
   `KeyboardCatInput`, `TouchStickCatInput` and `CatInputRouter` produce and merge `JumpPressed`
   (the latch, its reset, and the router merge). Interact must follow the same pattern.
b. `TouchStickCatInput`: current zone layout (stick region, jump rect, how a touch is assigned),
   and whether `F` is already bound by any keyboard component (GravityDebugControl uses Q/E/R,
   switch uses Tab, the panel uses backquote).
c. `LocalHumanDriver` and `InactiveDriver`: quote the per-tick method, and how the driver holds
   its `ObserverContext`.
d. `CatMotor2D` ground probe: does its `ContactFilter2D` ignore triggers? The vine zone is a
   trigger on the RealityA layer. If the probe can hit triggers, stop and report.
e. `RealityIsolationValidator`: does it flag references from a scene-root object (outside both
   RealityRoots) into a root, or from inside a root to a scene-root object? Presenters and
   `CatInteractor.transportHost` rely on both being allowed. If either is flagged, stop and report.
f. How `RealitySetup` / `TransportSetup` create placeholder sprites, colliders, layers and
   sorting layers (reuse the same helpers or approach).
g. `CatMotorConfig` jump speed field name and `GravityReceiver` strength field name (the setup
   script uses them to compute jump height).

Create — Parallax.Core (pure C#)
- Assets/_Game/Core/Anchors/EventOrigins.cs
  `public static class EventOrigins { public static EventOrigin Human(ObserverId id); }`
  A → HumanA, B → HumanB. Pure function, no state.
- Assets/_Game/Core/Anchors/IAnchorRequester.cs
  `public interface IAnchorRequester { EventOrigin Origin { get; } void Request(AnchorId anchor, float targetValue); }`
  `targetValue` is absolute.
- Assets/_Game/Core/Anchors/AnchorRequester.cs
  `public sealed class AnchorRequester : IAnchorRequester`
  - Constructor `(IRealityTransport transport, EventSequencer sequencer, EventOrigin origin)`;
    throws ArgumentNullException on null transport or sequencer.
  - `Request` builds `new AnchorRequest(anchor, targetValue, origin, sequencer.Next(origin))` and
    calls `transport.RequestAnchor`. Nothing else.
- Assets/_Game/Core/Anchors/AnchorToggle.cs
  `public sealed class AnchorToggle`
  - Constructor `(float initialValue)`; `float LastRequested { get; }` starts at initialValue.
  - `float Next()`: `LastRequested = LastRequested >= 0.5f ? 0f : 1f; return LastRequested;`
  - Toggles against the last *requested* value, not the committed one, so two quick presses under
    latency land opposite targets (same lesson as the PAX-019 debug button).

Create — Parallax.Gameplay
- Assets/_Game/Gameplay/Transport/TransportHost.cs
  `public abstract class TransportHost : MonoBehaviour` with
  `public abstract AnchorRegistry Registry { get; }`,
  `public abstract IRealityTransport Transport { get; }`,
  `public abstract EventSequencer Sequencer { get; }`.
  Gameplay code references `TransportHost`, never `LocalTransportHost`. FusionTransportHost will
  derive from it later. DebugTools may keep referencing `LocalTransportHost` (latency slider).
- Modify Assets/_Game/Gameplay/Transport/LocalTransportHost.cs
  - Derive from `TransportHost`.
  - Create Registry, LocalTransport and one `EventSequencer` **lazily** on first access of any
    of the three properties (a private EnsureCreated). Awake calls EnsureCreated too. This fixes
    Awake/OnEnable order: presenters access the registry in their own OnEnable, which may run
    before this host's Awake.
  - If `observers` is null, EnsureCreated logs the existing error once and the properties return
    null. `OnEnable` subscription to `Stepped` is unchanged.
  - `Local` stays (debug).
- Assets/_Game/Gameplay/Anchors/AnchorDefinition.cs
  ScriptableObject, `[CreateAssetMenu(menuName = "PARALLAX/Anchor Definition")]`.
  Fields: `int id` (valid 1..65534; 65535 is reserved for debug), `float initialValue` (0..1),
  `string displayName`. Property `AnchorId Id => new AnchorId((ushort)id)`.
- Assets/_Game/Gameplay/Anchors/RealityManifestation.cs
  `public abstract class RealityManifestation : MonoBehaviour { public abstract void SetTarget(float value, bool snap); }`
- Assets/_Game/Gameplay/Anchors/RealityPresenter.cs
  - Serialized: `AnchorDefinition definition`, `TransportHost transportHost`,
    `RealityManifestation[] manifestations`.
  - OnEnable: `Registry.Register(definition.Id, definition.initialValue)` (false is fine),
    `TryGet` and call `SetTarget(value, snap: true)` on every manifestation, subscribe to
    `Registry.Changed`. Log an error with context and return if any reference is missing.
  - OnDisable: unsubscribe.
  - On Changed for its own id: `SetTarget(state.Value, snap: false)` on every manifestation.
    Ignore other ids.
  - Never sends requests. Lives at scene root, outside both RealityRoots.
- Assets/_Game/Gameplay/Anchors/VineManifestation.cs (Reality A, visual only, no collider)
  - Serialized `Transform knot`, `float pullDistance = 0.6f`, `float speed = 3f` (units/s).
  - Moves `knot` along its local down between rest (value 0) and rest + pullDistance (value 1),
    `Mathf.MoveTowards` in FixedUpdate. `snap` sets it immediately.
- Assets/_Game/Gameplay/Anchors/ElevatorManifestation.cs (Reality B, carries a collider)
  - `[RequireComponent(typeof(Rigidbody2D))]`. Awake forces `bodyType = Kinematic`.
  - Serialized `Vector2 loweredLocal`, `Vector2 raisedLocal` (local to its RealityRoot, found with
    `GetComponentInParent<RealityRoot>()` in Awake; log an error if missing), `float speed = 2f`.
  - FixedUpdate: target = Lerp(lowered, raised, value); step toward it with `Mathf.MoveTowards`
    on position and `Rigidbody2D.MovePosition` (world = root position + local). `snap` sets
    `Rigidbody2D.position` directly.
  - Never computes the offset itself; the RealityRoot's position is the only source.
- Assets/_Game/Gameplay/Interaction/IInteractable.cs
  `public interface IInteractable { void Interact(ObserverId observer, IAnchorRequester requester); }`
- Assets/_Game/Gameplay/Interaction/CatInteractor.cs (on each cat)
  - Serialized `TransportHost transportHost`.
  - `public void Step(in CatCommand command, ObserverContext observer)`:
    - returns unless `command.InteractPressed`;
    - overlaps the cat's own `Collider2D` with a `ContactFilter2D` using
      `observer.Reality.PhysicsMask` and `useTriggers = true` (reused result list, no per-tick
      allocation);
    - picks the `IInteractable` (via TryGetComponent) whose collider center is closest to the cat;
      ignores colliders whose RealityRoot isn't `observer.Reality`;
    - calls `Interact(observer.Id, requester)`.
  - The requester is an `AnchorRequester(transportHost.Transport, transportHost.Sequencer,
    EventOrigins.Human(observer.Id))`, created lazily and cached per origin.
  - No input-device reads. No `Find`.
- Assets/_Game/Gameplay/Interaction/VineInteractable.cs (Reality A, on the trigger zone)
  - Serialized `AnchorDefinition definition`, `SpriteRenderer indicator`.
  - `AnchorToggle` created from `definition.initialValue` in Awake.
  - `Interact`: `requester.Request(definition.Id, toggle.Next())`, then start a 0.15 s local
    feedback pulse on `indicator` (tint, driven by `Time.time` in Update; cosmetic only).
  - The pulse shows immediately on press, before the commit. That's the causality theatre
    (§1.5, §9.4). The vine itself moves only on commit.
  - Never touches manifestations, the registry, or the transport.
- Modify Assets/_Game/Gameplay/Observers/LocalHumanDriver.cs
  - Get `CatInteractor` from the Observer's cat with `GetComponent` on Activate (cached; null means
    this cat can't interact).
  - In the per-tick method, immediately after the motor's Step, call
    `interactor.Step(in command, observer)` with the same command.
  - `InactiveDriver` is unchanged and never calls the interactor.
- Modify input (only as much as pre-read (a) shows is missing):
  - `KeyboardCatInput`: `F` → InteractPressed (edge latch, same pattern as jump) and InteractHeld.
  - `TouchStickCatInput`: an interact rect, normalized screen space, bottom-right, immediately
    left of the jump area. Starting proposal: interact = (0.56, 0.00, 0.18, 0.40); the jump area
    starts at x = 0.74. A touch that *begins* in the interact rect is never jump or stick.
    Same latch/reset pattern as jump; cleared in OnDisable and by the driver's transient reset.
    Draw the interact rect in the existing dev overlay. Report the before/after rects.
  - `CatInputRouter`: merge interact the same way it merges jump.

Modify — Parallax.DebugTools
- DebugPanel.cs: the debug anchor button uses `transportHost.Sequencer` instead of its own
  `EventSequencer` (one sequencer per device, D-025). Delete the panel's sequencer field.
  Nothing else changes.

Create — Parallax.Editor
- Assets/_Game/Editor/Setup/AnchorSetup.cs, menu `PARALLAX/Setup/Anchors (Sandbox)`.
  Idempotent by name. Uses RealitySpace names for layers and sorting layers. It creates or ensures:
  1. Asset `Assets/_Game/Data/Anchors/Anchor_Pathway01.asset`: id 1, initialValue 0,
     displayName "Pathway01" (AssetDatabase API; create folders if missing).
  2. Heights. `groundTop` = top of Reality A's Ground collider bounds, in root-local space.
     `jumpHeight = jumpSpeed^2 / (2 * gravityStrength)` from pre-read (g); if either value can't be
     read, use 2.0 and log a warning. `spawnX` = local x of Cat A's spawn.
  3. Reality A, under `RealityRoot_A/Anchors/`:
     - `Vine_A`: VineManifestation, child `Knot` with a placeholder sprite (A_Gameplay), local
       position (spawnX + 3, groundTop + 2.2). Vine visual only, no collider.
     - `VineZone_A`: VineInteractable, BoxCollider2D (isTrigger, size 1.2 x 1.6), layer RealityA,
       child indicator sprite (A_Gameplay, low alpha), centered at (spawnX + 3, groundTop + 0.8).
  4. Reality B, same local coordinates:
     - `RealityRoot_B/Anchors/Elevator_B`: ElevatorManifestation, kinematic Rigidbody2D,
       BoxCollider2D 2.0 x 0.4, sprite (B_Gameplay), layer RealityB, x = spawnX + 5.
       `loweredLocal`: top at groundTop + 0.4. `raisedLocal`: top at groundTop + 0.7 * jumpHeight.
     - `RealityRoot_B/Geometry/Ledge_B`: static BoxCollider2D + sprite (B_Gameplay), layer
       RealityB, width 3, height 0.5, left edge 0.3 right of the elevator's right edge, top at
       groundTop + 1.4 * jumpHeight (unreachable from the ground, reachable from the raised elevator).
     - If the ledge's right edge would pass B's Ground collider bounds, shift elevator and ledge
       left together and log it.
  5. Scene root `Anchors/Presenter_Pathway01`: RealityPresenter with the definition,
     `Systems/LocalTransportHost`, and manifestations [Vine_A, Elevator_B].
  6. `CatInteractor` on Cat A and Cat B, `transportHost` wired.
  7. `VineZone_A.definition` and `indicator` wired.
  Logs every change and every computed position; a second run logs "AnchorSetup: no changes."
  Then runs RealityIsolationValidator and AnchorValidator. Marks the scene dirty only if changed.
- Assets/_Game/Editor/Validation/AnchorValidator.cs (place next to RealityIsolationValidator if it
  lives elsewhere; say where), menu `PARALLAX/Validate/Anchors`, read-only. Checks:
  - every AnchorDefinition id is in 1..65534 and unique across all definition assets;
  - every RealityPresenter has a definition, a host and at least one manifestation, is not under a
    RealityRoot, and no two presenters share an id;
  - every manifestation is under a RealityRoot and referenced by exactly one presenter;
  - every VineInteractable has a definition, a trigger collider, is under a RealityRoot with a
    matching layer, and a presenter exists for its id;
  - every cat under a RealityRoot has a CatInteractor with a host.
  Logs "Anchors: OK" or one line per problem.

Create — Parallax.Tests.EditMode
- AnchorInteractionTests.cs:
  - EventOrigins: A → HumanA, B → HumanB.
  - AnchorToggle: initial 0 → 1, 0, 1. Initial 1 → 0. Initial 0.4 → 1. Initial 0.5 → 0.
  - AnchorRequester with a fake IRealityTransport (captures requests; declare its events with empty
    `add {} remove {}` accessors to avoid CS0067 warnings): stamps its origin, sequences 1, 2, 3,
    passes the target through unchanged.
  - Two requesters sharing one sequencer, different origins: each starts at 1.
  - Two requesters sharing one sequencer, same origin: sequences continue (1 then 2), never repeat.
  - Null transport or sequencer throws ArgumentNullException.
  - Integration with AnchorRegistry + LocalTransport (tick source as in LocalTransportTests):
    request during tick 5 is not committed by Pump(5), committed by tick 6. At 300 ms (15 ticks at
    dt 0.02), two toggled requests in the same tick → after both deliveries value is back to the
    initial value and Revision is 2.

Docs
- 02_ARCHITECTURE.md
  - §4: LocalHumanDriver calls `CatInteractor.Step` with the same command, right after the motor.
  - §5.1: interact input (F; touch interact rect left of jump).
  - §7.2: "As built (PAX-021)" paragraph: TransportHost, presenters at scene root registering and
    snapping in OnEnable, manifestations animating in FixedUpdate (elevator: kinematic,
    MovePosition), AnchorDefinition assets, CatInteractor → IInteractable → IAnchorRequester.
    Replace "issue AnchorRequests through the transport" with "issue AnchorRequests through the
    IAnchorRequester handed to them by the interacting cat (D-025)".
- CLAUDE.md, Semantic state section: change "Puzzles talk only to `IRealityTransport`" to
  "Puzzles talk only to `IRealityTransport`; interactables go through the `IAnchorRequester` their
  cat hands them (D-025)". No other CLAUDE.md change.
- 07_DECISIONS.md: append
  ### D-025 · <date> · Accepted
  **Decision:** (1) Interaction is driven by the Observer's `CatCommand`: `LocalHumanDriver` calls `CatInteractor.Step` with the command it gave the motor. Interactables never read input devices. (2) Interactables never touch the transport, registry, or manifestations. They receive an `IAnchorRequester` from the interacting cat, which stamps the Observer's origin and a sequence from the device's single `EventSequencer` on `TransportHost`. (3) Presenters live at scene root, outside both RealityRoots; they register their anchor, snap manifestations on enable, and forward committed values. Manifestations animate locally in FixedUpdate and may carry colliders. (4) Anchor IDs come from `AnchorDefinition` assets (1..65534; 65535 reserved for debug). (5) Gameplay references the abstract `TransportHost`, never `LocalTransportHost`.
  **Why:** Reading input in the interactable would let the active human trigger an inactive cat's zone. A per-Observer requester is the interception point Echo recording needs (§8.3) and keeps origin and sequencing out of puzzle code. Two sequencers issuing the same origin would collide under D-024. Presenters outside the roots keep the isolation rule (no A↔B references).
  **Supersedes:** 02_ARCHITECTURE §7.2 "issue AnchorRequests through the transport"; the CLAUDE.md line "Puzzles talk only to IRealityTransport" (narrowed, not removed).

---

YOU — UNITY EDITOR (the Architect will walk you through these one at a time)
1. Recompile; zero Console errors. Run EditMode tests: 75 + new, all green.
2. Open Sandbox_Realities. Run `PARALLAX/Setup/Anchors (Sandbox)` twice (second run → no changes).
   Both validators OK. Save.
3. Run the Editor acceptance tests.
4. Build to the Pixel 8a and run the device tests.

DO NOT
- Add Echo recording or playback, a Hold vine mode, pressure plates, Control Stations, or
  gravity control streams (PAX-023/024, Phase 5).
- Reset anchors on respawn (checkpoint ticket).
- Predict manifestation movement before commit. Only the interactable's pulse is immediate.
- Add multi-writer rules for one anchor (more than one interactable per anchor).
- Change motor, camera, switch, reality or transport-delivery code. If the cat does not ride the
  elevator cleanly, report it with what you observed; do not patch the motor in this ticket.
- Reference Photon or add Net.Fusion code.
- Add singletons, static mutable state, or runtime `Find`/`FindObjectOfType`. `GetComponent` and
  `GetComponentInParent` on an object's own hierarchy are allowed.
- Hand-edit .unity/.prefab/.asset/.meta/ProjectSettings. Assets are created only by the setup script.
- Commit. The Architect reviews first.

REQUIREMENTS
- Request path: InteractPressed in the Observer's command → CatInteractor overlap (own reality
  mask) → IInteractable → IAnchorRequester → IRealityTransport → Pump → AnchorRegistry.Commit →
  Changed → RealityPresenter → manifestations. No shortcut around any step.
- An Inactive cat standing in a zone can never be triggered by the active human's input.
- The interactable's feedback pulse starts on the press tick; the vine and elevator move only
  after the commit.
- One EventSequencer per device, owned by TransportHost.
- Presenters register and snap on enable, regardless of Awake order.
- All physics queries use the own-reality mask. Cats never collide with the other reality's
  elevator, ledge, or zone.
- Validators pass. Setup is idempotent.
- All new pure logic is covered by EditMode tests, with zero new Console warnings.

ACCEPTANCE TEST

Editor
1. EditMode: all tests green (75 + new).
2. Play, latency 0, panel open. Before interacting, the anchor table shows Anchor#1 value 0.00
   rev 0 (registered by the presenter).
3. As A, press F away from the vine: nothing (rev unchanged). Walk into the vine zone; the cat
   walks through it and does not stand on it. Press F: the zone pulses, the vine knot moves down,
   the table shows value 1.00 rev 1.
4. Tab to B: the elevator is raised. Jump onto it, then onto the ledge. From the ground, the ledge
   is not reachable by jumping.
5. Carry: jump down, walk onto the elevator. Tab to A, press F in the zone (elevator lowers with the
   inactive cat B on it). Press F again (it rises). Tab to B: B is on the raised elevator, not
   fallen through or pushed off.
6. Isolation: as A, walk through the elevator's and ledge's location (no collision). As B, stand
   at the vine zone's location and press F: nothing.
7. Inactive guard: move A into the vine zone, Tab to B, press F: nothing (rev unchanged).
8. Latency 300: press F in the zone as A. The pulse is immediate; PendingCount shows 1; the vine
   moves about 0.3 s later.
9. At 300 ms, press F twice quickly: both commit in order, value ends where it started, rev +2.
10. At 300 ms, press F and Tab immediately: the request still commits; B watches the elevator move.
11. Movement, jumping, switching, gravity debug and the debug anchor button behave as before.
    Zero Console warnings or errors.

Device (Pixel 8a, latency 300)
12. Tap the interact rect in the vine zone: immediate pulse, delayed vine; Tab-equivalent SWITCH →
    elevator raised; complete test 4 with touch.
13. Tapping interact never jumps or moves the stick; the stick and jump still work.
14. Tapping interact outside a zone does nothing. Tapping DBG, the panel or SWITCH never interacts.

DELIVERABLE (from Claude Code)
Standard CLAUDE.md output, plus:
- the pre-read report (a–g);
- a tick trace at 300 ms: on which tick F is read, the request is made, it's delivered, Changed
  fires, and the elevator first moves;
- the before/after touch rects;
- every position the setup script computes (from its log, or the formula if untested);
- grep proof, output pasted verbatim:
    grep -rn "Photon\|Fusion" Assets/_Game
    grep -rn "LocalTransportHost" Assets/_Game/Gameplay --include=*.cs
    grep -rn "Find" Assets/_Game/Core Assets/_Game/Gameplay
    grep -rn "static" Assets/_Game/Core/Anchors Assets/_Game/Gameplay/Anchors Assets/_Game/Gameplay/Interaction
  (LocalTransportHost may appear only in its own file and TransportHost-related comments.
  For each "static" hit, say whether it is mutable state.)
- the D-number used;
- the new EditMode test count.
- Review file, written before stopping:
    { git status; git --no-pager diff; cat <every new .cs file> Docs/TASKS/PAX-021.md; } > ~/Desktop/pax021_review.txt 2>&1

DEFINITION OF DONE
Universal DoD, plus:
- You can explain in two sentences why interaction must use the Observer's command instead of
  reading input devices, and why presenters live outside both RealityRoots.
- Device tests 12–14 pass at 300 ms.
- Architect review passed before commit. The commit includes all new .meta files, the Anchor_Pathway01
  asset and the saved scene:
  `PAX-021: presenters, manifestations, cross-reality vine to elevator (absorbs PAX-022)`
