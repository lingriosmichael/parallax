TASK: PAX-010
TITLE: Gravity debug control (90° steps) + body alignment + gravity test arena
PHASE / GATE: Phase 2 (One ugly cat) / Gate 2
NOTE: PAX-009 was merged into PAX-008 (D-019).

OBJECTIVE
A debug key rotates THIS cat's gravity in 90° steps. The cat smoothly aligns its body to the new
"down", and movement, jumping and grounding work in all four gravity directions.

CONTEXT
- 02_ARCHITECTURE.md §6 (GravityReceiver, motor sketch incl. body.rotation line), §2 (Parallax.DebugTools),
  §3.2 (camera stays world-aligned; open question Q-2).
- CLAUDE.md: Gravity ("Only GravityReceiver.SetTargetDirection changes a cat's gravity"),
  Unity file rules, Echo (no Time.time). D-006, D-012, D-019.
- Existing: CatMotor2D (ground detection, jump, coyote, buffer), GravityReceiver (turns toward target at
  turnSpeedDegPerSec, default 360), Cat_Player prefab (Rigidbody2D Freeze Rotation Z, capsule 1.2 x 0.8),
  CatPlayerSetup.cs (PARALLAX/Setup/Configure Cat Player), Sandbox_Player scene containing a GameObject
  named "Ground" (square sprite, BoxCollider2D, position (0,-4), scale (18,1)).
- Camera: orthographic size 5, world-aligned. Visible area ≈ x -8.9..8.9, y -5..5 at 16:9.

CLAUDE — CODE
Allowed files (nothing else):

1. CREATE Assets/_Game/Core/Physics/GravityMath.cs   (namespace Parallax.Core)
   - `public static Vector2 Rotate90(Vector2 dir, int steps)`: rotates by steps * 90°, positive =
     counter-clockwise; result snapped to exact axis values (no floating drift), normalized.

2. CREATE Assets/_Game/Tests/EditMode/GravityMathTests.cs
   - down +1 → right (1,0); down −1 → left (−1,0); down +2 → up; +4 → down; −3 equals +1; results exact.

3. MODIFY Assets/_Game/Gameplay/Player/CatMotor2D.cs
   - At the end of FixedUpdate, align the body so its local up equals −gravity.Direction:
     `body.rotation = Vector2.SignedAngle(Vector2.down, down)` (as in §6). Smoothness comes from
     GravityReceiver turning gradually; do not add a second smoothing layer.
   - Keep Freeze Rotation Z untouched. Keep all PAX-007/008 behaviour.
   - If setting body.rotation breaks interpolation or causes visible jitter, report it; do not switch
     approach silently.

4. CREATE Assets/_Game/DebugTools/Parallax.DebugTools.asmdef
   - name `Parallax.DebugTools`, rootNamespace `Parallax.DebugTools`,
     references `Parallax.Core`, `Parallax.Gameplay`, `Unity.InputSystem`,
     defineConstraints `["UNITY_EDITOR || DEVELOPMENT_BUILD"]`, autoReferenced false.

5. CREATE Assets/_Game/DebugTools/GravityDebugControl.cs   (namespace Parallax.DebugTools)
   - `[RequireComponent(typeof(GravityReceiver))]`. Input System keyboard (no legacy Input):
     Q = rotate this cat's gravity target 90° counter-clockwise, E = 90° clockwise, R = reset to down.
   - Tracks its own step index; calls ONLY `GravityReceiver.SetTargetDirection(GravityMath.Rotate90(Vector2.down, steps))`.
   - Logs one line per change, e.g. "GravityDebug: Cat_Player gravity → (1, 0)".

6. MODIFY Assets/_Game/Editor/CatPlayerSetup.cs
   - ADD a second menu item `PARALLAX/Setup/Build Gravity Test Arena` (do not change Configure Cat Player).
   - Works on the ACTIVE scene. Requires a GameObject named "Ground"; if missing, log an error and stop.
   - Idempotent: if a root GameObject "GravityArena" exists, log "already built" and stop.
   - Creates root "GravityArena" and, by duplicating "Ground" (same sprite, colour, BoxCollider2D),
     three children: Wall_Left at (−8.5, 0) scale (1, 9); Wall_Right at (8.5, 0) scale (1, 9);
     Ceiling at (0, 4) scale (18, 1). Register Undo, mark the scene dirty, do not save the scene.
   - Parallax.Editor.asmdef may need no change; if it does, STOP and report instead of editing it.

DO NOT
- Touch .unity, .prefab, .asset, .meta, ProjectSettings/, Packages/, Docs/ by hand.
- Add GravityDebugControl to the prefab (it is added to the scene cat only, by the developer).
- Rotate the camera, add touch/tilt/dial input, Control Stations, fall reset, layers, Observers, networking.
- Use or modify Physics2D.gravity. Change gravity from anywhere except SetTargetDirection. Use Time.time.
- Run git commands. Create files outside the allowed list.

REQUIREMENTS
- Gravity direction changes only through GravityReceiver.SetTargetDirection.
- The body rotates smoothly (≈0.25 s per 90° at default turn speed) and ends exactly aligned.
- Walking, jumping, grounding, coyote time and buffering work with gravity down, right, up and left.
- GravityDebugControl and the DebugTools assembly are excluded from release builds.
- EditMode tests pass.

YOU — UNITY EDITOR (after Architect review)
1. Compile; no red errors.
2. Open Sandbox_Player → run PARALLAX → Setup → Build Gravity Test Arena → save the scene (Cmd+S).
   Run it again: "already built".
3. Select the scene's Cat_Player → Add Component → GravityDebugControl → save the scene.
4. Test Runner → EditMode → Run All: all tests green.

ACCEPTANCE TEST (Editor only)
1. Play. Gravity down: walking and jumping as before.
2. Press E: the cat turns smoothly and falls to the new floor; it walks (A/D) and jumps along that wall.
3. Repeat E until gravity has pointed in all four directions (down → left → up → right or reverse); at each:
   stand, walk both ways, jump, land, and confirm no double jump.
4. Q rotates the other way; R returns to normal gravity.
5. Change gravity while standing, while walking, and mid-jump: no errors, no getting stuck inside walls
   (brief pushing out of a wall when turning next to it is acceptable — report if frequent).
6. No red Console errors.

KNOWN / EXPECTED
- The camera does not rotate, so controls are relative to the cat (on the right wall, D moves up the screen).
  Whether the camera should rotate is open question Q-2 — observe how it feels and report.

DELIVERABLE (from Claude)
Per CLAUDE.md "Required output"; say clearly what was and was not observed in Unity.

DEFINITION OF DONE
Universal DoD + the developer can explain why the motor needs no changes to walk on walls
(movement is computed relative to gravity).
