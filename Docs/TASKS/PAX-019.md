TASK: PAX-019 (absorbs PAX-020)
TITLE: Semantic core: event types, AnchorRegistry, IRealityTransport, LocalTransport with artificial latency
PHASE / GATE: Phase 4 / toward Gate 3

OBJECTIVE
The data layer for everything cross-reality. Anchors can be requested, committed by the
authority, and observed, through a transport that behaves like a network (never synchronous,
configurable delay) while running entirely on one device. No gameplay objects use it yet
(PAX-021/022 add the vine and elevator). This ticket is proven by EditMode tests and a debug
anchor in the debug panel.

CONTEXT
- 02_ARCHITECTURE.md §7 (Semantic state: anchors, control streams, cues), §9 (Transport),
  §9.1 (LocalTransport), §10 (Time), §13 (Debug tooling).
- D-022 (ObserverSet owns Tick), D-023 (debug panel, reserved touch regions), Q-8
  (Echo scope, decided before PAX-023).
- Current state: Phase 3 complete and verified on the Pixel 8a. Sandbox_Realities is the
  working scene and the only scene in the build.
- Plan PAX-020 is merged: the registry can't be tested meaningfully without the request types,
  and the transport can't be tested without the registry.

---

STEP 0 — BEFORE CLAUDE CODE STARTS
Commit the build scene-list change from the device session (ProjectSettings/EditorBuildSettings.asset):
`PAX-017 device pass: Sandbox_Realities in build; Phase 3 verified on Pixel 8a`.

---

CLAUDE — CODE

Pre-read and report before editing: ObserverSet (FixedUpdate order), DebugPanel (PanelRect
and how ContainsScreenPoint uses it), and the Parallax.Core / Parallax.Gameplay asmdefs.
Stop and report if anything contradicts this ticket.

Create — Parallax.Core (pure C#, no MonoBehaviours)
- Assets/_Game/Core/Anchors/AnchorId.cs — as §7.1, plus `ToString()` → "Anchor#<value>".
- Assets/_Game/Core/Anchors/AnchorState.cs — as §7.1 (`float Value`, `uint Revision`).
- Assets/_Game/Core/Anchors/EventOrigin.cs — `HumanA, HumanB, EchoA, EchoB, System`.
- Assets/_Game/Core/Anchors/AnchorRequest.cs — as §7.1, with a constructor. The target is
  **absolute**, never a delta.
- Assets/_Game/Core/Anchors/CommitResult.cs — `Applied, Duplicate, NoChange, UnknownAnchor, InvalidValue`.
- Assets/_Game/Core/Anchors/AnchorRegistry.cs — pure C# class:
  - `bool Register(AnchorId id, float initialValue)`: creates the anchor with Revision 0;
    returns false (no change) if it already exists.
  - `bool TryGet(AnchorId id, out AnchorState state)`.
  - `CommitResult Commit(in AnchorRequest request)`, authority side. Checks in this order:
    1. unregistered anchor → UnknownAnchor;
    2. NaN or infinite target → InvalidValue;
    3. `request.Sequence <= lastAppliedSequence[request.Origin]` → Duplicate;
    4. otherwise record `lastAppliedSequence[Origin] = Sequence`, then:
       - `Value == TargetValue` (exact) → NoChange (no revision change, no event);
       - else set Value, Revision++, raise `Changed(id, state)` → Applied.
  - `void ApplyReplicated(AnchorId id, AnchorState state)`, non-authority side: ignore if
    `state.Revision <= current.Revision`; create the anchor if unknown; raise Changed when applied.
  - `event Action<AnchorId, AnchorState> Changed`.
  - `IEnumerable<KeyValuePair<AnchorId, AnchorState>> All`, for the debug table, in stable
    registration order.
  - `void ResetSequences()`, which clears lastAppliedSequence (checkpoint/level reload later).
    Not called yet.
- Assets/_Game/Core/Anchors/EventSequencer.cs — per-origin monotonic counter:
  `uint Next(EventOrigin origin)`. It starts at 1, so 0 can never pass the duplicate check
  against a fresh registry.
- Assets/_Game/Core/Streams/ControlChannel.cs, ControlSample.cs — as §7.3, with constructors.
- Assets/_Game/Core/Streams/SpectacleCue.cs — as §7.4, with a constructor.
- Assets/_Game/Core/Transport/IRealityTransport.cs — as §9.
- **No** abstract `RealityEvent` base type. The plan's "RealityEvent types" are the concrete
  structs above.

Create — Parallax.Gameplay
- Assets/_Game/Gameplay/Transport/LocalTransport.cs — pure C# class implementing IRealityTransport.
  - Constructor: `(AnchorRegistry registry, float fixedDeltaSeconds)`.
  - `IsSessionAuthority` is always true.
  - `Tick` is the tick of the last `Pump`.
  - `LatencyMs { get; set; }`, clamped to 0..400.
  - `RequestAnchor`, `PublishControl`, `SendCue` **enqueue**. They never deliver inside the
    call, even at 0 ms, so no caller can come to rely on synchronous commits the network
    won't give.
  - Delivery tick = `Tick + max(1, ceil(LatencyMs / (fixedDeltaSeconds * 1000)))`, computed
    when the item is enqueued.
  - `void Pump(int tick)`: sets Tick, then delivers every queued item with deliveryTick ≤ tick,
    in enqueue order:
    - anchor requests → `registry.Commit`;
    - control samples → `ControlReceived`;
    - cues → `CueReceived`.
  - Changing LatencyMs never reschedules items already queued.
  - `int PendingCount` (debug).
  - `CommitResult LastCommitResult` (debug).
- Assets/_Game/Gameplay/Transport/LocalTransportHost.cs — MonoBehaviour.
  - Serialized `ObserverSet observers`, `int initialLatencyMs = 0`.
  - Awake: creates the AnchorRegistry and a LocalTransport (`Time.fixedDeltaTime`).
  - Exposes `Registry`, `Transport` (as IRealityTransport), and `Local` (the LocalTransport,
    for debug).
  - Subscribes to `ObserverSet.Stepped` and calls `Pump(tick)` there.
  - No singleton or static access.
- Modify ObserverSet.cs:
  - Add `event Action<int> Stepped`, raised at the end of FixedUpdate, after A and B stepped.
  - Order per tick: `Tick++` → A.Step → B.Step → Stepped(Tick).
  - No other change.

Modify — Parallax.DebugTools
- DebugPanel.cs:
  - Serialized `LocalTransportHost transportHost`.
  - When open, add:
    - a latency slider 0–400 ms (label shows ms and the equivalent ticks);
    - PendingCount;
    - LastCommitResult;
    - an anchor table (ID, Value, Revision) from `Registry.All`;
    - a "Debug anchor 0↔1" button.
  - The button registers `AnchorId(65535)` (reserved for debug; initial value 0) on first use,
    then requests the opposite of its current value with origin System and
    `EventSequencer.Next(System)`, using a sequencer owned by the panel.
  - Grow the panel rect to fit.
  - **ContainsScreenPoint must use the same rect the panel draws**: one field, not two copies.
  - The panel still never assigns drivers.

Create — Parallax.Editor
- Assets/_Game/Editor/Setup/TransportSetup.cs — menu `PARALLAX/Setup/Transport (Sandbox)`.
  Must be idempotent. When run, it:
  - ensures a scene-root `Systems` GameObject (per §3.1) with LocalTransportHost wired to ObserverSet;
  - wires `DebugPanel.transportHost`;
  - runs RealityIsolationValidator;
  - logs a summary; a second run logs "no changes".

Create — Parallax.Tests.EditMode
- AnchorRegistryTests.cs:
  - Register then TryGet → Value initial, Revision 0.
  - Registering twice returns false and leaves the state unchanged.
  - Commit applies: value set, Revision 1, Changed raised exactly once with the new state.
  - Same (Origin, Sequence) twice → second is Duplicate, no revision change, no event.
  - Older sequence after newer (5 then 3) → Duplicate; value stays at seq 5's target.
  - Different origins with the same sequence number are independent.
  - Target equal to current → NoChange, no revision change, no event, but the sequence is
    recorded (re-sending it is Duplicate).
  - Unknown anchor → UnknownAnchor. NaN target → InvalidValue, and the sequence is **not** recorded.
  - Three applied commits → Revision 3.
  - ApplyReplicated: newer revision applied + event; equal and older ignored, no event;
    unknown anchor created.
  - EventSequencer: starts at 1, per-origin independent, monotonic.
- LocalTransportTests.cs (dt = 0.02):
  - At 0 ms, RequestAnchor does not commit before Pump; Pump(tick+1) commits.
  - At 100 ms, delivered at tick+5, not at tick+4.
  - At 400 ms, delivered at tick+20. Setting 1000 clamps to 400; −5 clamps to 0.
  - Two requests enqueued at the same tick are delivered in enqueue order (second target wins).
  - An item enqueued at 200 ms keeps its delivery tick when latency drops to 0; a later item
    enqueued at 0 ms may arrive first. Assert both delivery ticks.
  - PublishControl raises ControlReceived after the delay; SendCue raises CueReceived after the delay.
  - IsSessionAuthority is true; Tick equals the last pumped tick; PendingCount decreases on delivery.

Docs
- 02_ARCHITECTURE.md
  - §7.1: registry signature (`CommitResult`, `Register`) and the per-origin sequence rule.
  - §9.1: never-synchronous delivery, delivery-tick formula, pumping via `ObserverSet.Stepped`.
  - §13: latency slider and anchor table.
- 07_DECISIONS.md: append
  ### D-024 · <date> · Accepted
  **Decision:** (1) Duplicate protection is per-origin highest-applied sequence (`Sequence <= last` → Duplicate), not a bounded recent-ID set. (2) `AnchorRegistry.Commit` returns `CommitResult`; anchors must be `Register`ed with an initial value. (3) `LocalTransport` never delivers synchronously: minimum one tick, delay computed in ticks at enqueue, pumped once per tick after both Observers step.
  **Why:** A recent-ID window lets a late duplicate outside the window re-apply an outdated absolute target and revert a newer change; per-origin ordering rejects it and is bounded by the number of origins. Synchronous local delivery would let gameplay code silently depend on behavior the network can't provide. Tick-based delay keeps delivery deterministic for Echo.
  **Consequence for PAX-023/024:** an Echo replay must issue fresh sequences from its own origin's sequencer on every playback, never re-send recorded sequence numbers, or the second replay is dropped as Duplicate.
  **Supersedes:** 02_ARCHITECTURE §7.1 "bounded recent-ID set" and `bool Commit`.

---

YOU — UNITY EDITOR
1. Recompile; zero Console errors.
2. Open Sandbox_Realities. Run `PARALLAX/Setup/Transport (Sandbox)` twice (second run →
   "no changes"). Save.
3. Run the acceptance tests.

DO NOT
- Add the vine, elevator, presenters, manifestations, or interactables (PAX-021/022).
- Add Echo recording or playback (PAX-023/024).
- Reference Photon, or add FusionTransport or any Net.Fusion code.
- Apply control samples to GravityReceiver (Control Station is later).
- Add singletons, static service locators, or `FindObjectOfType` lookups at runtime.
- Change motor, input, switch, or reality code (beyond the ObserverSet event).
- Hand-edit .unity/.prefab/.asset/.meta/ProjectSettings.

REQUIREMENTS
- Core contains no MonoBehaviours and no Unity API beyond UnityEngine math/logging.
- Nothing is delivered synchronously from any transport send method.
- Per tick: Tick++ → A.Step → B.Step → Stepped → Pump. One place, in that order.
- All registry and transport behavior above is covered by EditMode tests.
- Debug panel rect and reserved-region rect are the same field.
- Setup is idempotent; the isolation validator still passes.

ACCEPTANCE TEST

Editor
1. EditMode: all tests pass (existing 48 + new).
2. Play. Open the debug panel (`). Latency 0. Press "Debug anchor 0↔1".
   The table shows Anchor#65535 at 1, Revision 1, LastCommitResult Applied. Press again → 0, Revision 2.
3. Set latency to 400. Press the button → PendingCount 1 for about 0.4 s, then value flips and
   Revision increments.
4. At 400 ms, press the button twice quickly (within 0.4 s). Both are delivered in order; the final
   value is back where it started, Revision +2.
5. Switch cats (Tab) while a request is pending → it still commits. The anchor doesn't belong to
   an Observer.
6. Stepping, switching, and movement are unchanged from Phase 3.
7. Zero Console errors.

Device (short)
8. Build. Open DBG, set latency 300, tap the debug anchor button → visible delay, then commit.
9. Tapping the slider or the button never moves or jumps the cat (reserved region grew with the panel).

DELIVERABLE (from Claude Code)
Standard CLAUDE.md output. Also include:
- the pre-read report;
- the per-tick order as implemented (quote the ObserverSet FixedUpdate);
- grep proving no Photon references and no statics or singletons in the new code;
- the D-number used.

DEFINITION OF DONE
Universal DoD, plus:
- You can explain in two sentences why LocalTransport must never commit inside RequestAnchor,
  and why an Echo replay needs fresh sequence numbers.
- Review file sent to the Architect before commit:
  `{ git status; git --no-pager diff; cat <new files>; } > ~/Desktop/pax019_review.txt 2>&1`
- Commit: `PAX-019: anchors, registry, IRealityTransport, LocalTransport + latency`
